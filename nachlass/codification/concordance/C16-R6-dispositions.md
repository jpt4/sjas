# C16 — R6 Drift Disposition Audit

Walk of every entry in [`drift-ledger.md`](drift-ledger.md) against
[`../codified-sjas.md`](../codified-sjas.md), per
[`../C16-verification-goal.md`](../C16-verification-goal.md) §4 R6 and
acceptance criterion **A-D**. Targets deficiency **11** — *components leave
their own obligations open* — whose named precedent is **D29**.

Audit date: 2026-08-31. Nothing committed.

---

## 1. Which D ids exist

The goal document says "D1–D80". **Sixty-seven** entries exist. Thirteen ids
were never issued and are unused gaps, all inside the 52–69 band the task
anticipated:

- **Present (67):** D1–D51, D53–D57, D70–D80.
- **Unused gaps (13):** **D52**, **D58–D69**.

Plus one non-numbered heading, **`D1-supplement`** (`Willard2016` corroboration
of the April-1994 Solovay dating), which carries no status line of its own and
is dispositioned below with D1.

`audit.sh` check **M6** (id-continuity *report*, not failure) should be pointed
at this band so the gap stays visible rather than being rediscovered.

Ledger heading order is not numeric — D1, D7–D14, D2–D6, D15–D48, D51, D49,
D1-supplement, D50, D53–D57, D70–D80. Harmless, but a reader scanning for a
number will miss D2–D6 and D49–D51 unless warned.

---

## 2. Dispositions

`Prior status` is verbatim from the ledger's own `Status:` line.
Section references in `Where / reason` are to `../codified-sjas.md` unless
another file is named.

| Id | Prior status | Disposition | Where / reason |
| --- | --- | --- | --- |
| D1 | explained:2026-08-21 | resolved | TR 93-10's date/page identity closed by the `[So94]` April-1994 citation (G9 closed). No presentation obligation: §2.4 cites `Willard1993-TR` p. 2 for the two-branch derivation and makes no dating or page-count claim. |
| D1-supplement | *(no status line)* | resolved | `Willard2016` Ex. 3.3's independent confirmation of the 1994 dating is carried at §2.4 ("telephone conversations during April 1994"), together with `Willard2001` Appendix A as the sole extended exposition. |
| D2 | open | carried | `Willard2006a`'s pre-publication title ("new variant of Hilbert styled…"). Bibliographic identity only; codified-sjas.md never cites the paper by title, so no reader-facing both-readings obligation arises. Carried to Refinement at the `registry/corpus.md` level. **Note:** the entry's assigned action ("confirm during the C9 extraction") is not recorded in `../extraction/willard2006a.md`. |
| D3 | open (C6 confirmation pending) | remains-open | **Open because** the C6 cross-paper confirmation is still unrecorded: the entry asks whether `Willard2011` Def. D.1(iv) (Gödel number ≥ 32^J) and `Willard2005` Eq. (20) are one condition under two presentations. **Both readings:** §6.6 separates the constants in a three-row table (1/5 density, 1/4 proved θ, 1/3 ceiling) and names Appendix A's six-bit byte as the density's source. **Shortfall:** §6.6's triple is 1/5 · 1/4 · 1/3, not D3's 6 · 5 · 1/4, and `Willard2011` D.1(iv) appears nowhere; O2 remains undischarged in the document. |
| D4 | explained:2026-08-21 | resolved | Solovay provenance chain fully carried at §2.4 (April-1994 telephone communications, never published, `Willard2001` App. A the only extended exposition, four-page and weaker than `Willard2006a` Thm 2), and marked *cited, unpublished* in §8's Type-S row. Obligations O5, O75 cited at the point of use. |
| D5 | open | **remains-open — finding** | **Open because** no single statement of the `IS(A)` / `IS_D(A)` delta exists (O7). **Both readings: not presented.** The two names are kept typographically distinct (§5.1, §6.9) and the Level(0−)/Level(1) difference is separately inferable from §5.2's table, but the delta — Level, apparatus parameter, and the Group-1/appendix delegation from 2005 back to 2001 — is never stated in one place, and D5 is not cited anywhere in codified-sjas.md. |
| D6 | open (carry into C14–C15 composition) | resolved | Discharged at §6.6: drift **D6** cited by id, the window `1/d ≤ θ < 1/3` stated, `ADR-0001`'s inverted "θ ≤ 1/5" named as the error, obligation O3 cited. The entry's second limb (Theorem 1's `m ≥ 3`) is discharged at §6.4 with O1; its third (the cut-free-sequent proof-status caveat) at §6.8. Ledger status is stale and should move to `explained:2026-08-31`. |
| D7 | open | **remains-open — finding** | **Open because** the corpus's three closely-named, non-equivalent predicates are never tabulated (O25). **Both readings: not presented.** §2.1–§2.2 fix one vocabulary (`Willard2016` Def. 3.4's *self-justifying configuration*), which is half of what D7 asks, but 1993's *self-verifying* and 2005's *Introspectively Unified Logic* / *Self-Justifying* (∃d) appear nowhere: `self-verifying` and `Introspectively` both return zero hits. D7 uncited. |
| D8 | open | **remains-open — finding** | **Open because** `IS*(A)` (`Willard1993` p. 330) versus `IS^{Σ₁}(A)` (`Willard1993-TR` p. 9) is unadjudicated. **Both readings: not presented.** §9.3 writes `IS*_+(A)` — precisely the notation the entry warns collides with the starred *formula classes* of §3.3 — with no note and no citation of D8. |
| D9 | open — the codified statement must adopt one and note the other | **remains-open — finding** | **Open because** neither 1993 definition of the formula classes is adopted on the record. **Both readings: not presented.** §3.3 handles the *notational* half (the closing paragraph: "`Willard1993-TR` writes `Δ₀/Π₁/Σ₁` for what 2005 writes starred"), but `Willard1993` p. 327's majorization-style condition — "less than or equal to the maximum of the universally quantified variables enclosing it" — is absent, so the TR-versus-chapter divergence D9 records is invisible. D9 uncited; O12 half-discharged. |
| D10 | open | remains-open — finding (mild) | **Open because** the fourteen-function chapter list and the eight-function TR `G₀` list are not reconciled on the page. **Both readings: not presented.** §3.2's table carries only the 2002c/2016 six, the 2006a seven and the 2005 eight; neither 1993 list appears. The entry's *relativizer* is carried — §3.3 closes with `Willard1993-TR` Remark 6 and O13, "it is the **growth class**, not the signature, that is load-bearing" — which is the substance, so the omission misleads no one. |
| D11 | open (reduced to a presentational obligation, O10/O13) | remains-open (mild) | **Open because** O10's "neither list is canonical" is never written. **Both readings: not presented** — but the trap cannot spring: only one eight-function list (`Willard2005` U-Grounding) appears in §3.2, so the coincidence-of-cardinality error D11 guards against has no purchase in this document. O10's rule ("never write 'Willard's eight functions' without naming the source") is honoured throughout. |
| D12 | open — a genuine explanatory point the codified statement should make explicit | **remains-open — finding** | **Open because** the load-bearing consequence is still unstated. **Both readings: not presented.** §3.2 gives the premise ("addition is total there, so it may be a function symbol") and §6.3 consumes the conclusion (Normed clause 1 requires every axiom Π\*₁/Σ\*₁), but the link — 1993's addition-totality is an explicit **Π₂** axiom, and the U-Grounding function language exists to make it Π\*₁ — is never drawn. O11 undischarged; D12 uncited. |
| D13 | open (composition obligation O9) | **remains-open — finding** | **Open because** the attribution correction is unmade. **Both readings: partially presented.** §7.2 cites `Willard1993` pp. 328–331 as an earlier statement of the multiplication trigger, but does not present the *duplication* framing ("two occurrences of the same variable"; "the same double appearance of a variable") nor its link to Gödel's diagonalization, which is the whole of O9. The correcting edit to the affine-tree documents is out of scope by the entry's own last bullet; the in-scope action is the attribution sentence, and it is absent. |
| D14 | open | carried | Constant-pointer compression (`u#`, `v#` as totality devices; TR p. 18's dependency of Lemma 5.3, and hence Propositions 2b, 2c, 4 and 6, on the symbol `u#`). Carried: codified-sjas.md treats the TR at the grain of Propositions 1 and 7 only (§2.4) and makes **no** compression or sharing claim in either direction, so no both-readings failure exists. `pointer` returns zero hits. Refinement should state the device when it treats the TR encoding. |
| D15 | open (composition obligation O14) | **remains-open — finding** | **Open because** only one of the two routes is given. **Both readings: not presented.** Reading 2 (2005 Remark 4's envelope arithmetic) is presented in full at §6.7. Reading 1 (`Willard1993-TR` §12: multiplication-totality makes local Π₁ reflection derivable via Lemma 12.1, which Proposition 5 has shown inconsistent) is absent — §7.2 presents a *third* framing, the dying-lemma collapse of the negative proofs, which is not D15's reading 1. O14 requires both routes to one boundary; one is missing. |
| D16 | open | resolved | §5.4 names *axiom-versus-theorem under a cut-free apparatus* once as a mechanism and refers back to it (§2.2 clause (i), §3.1's placement of Type-Almost-M, §7.3's first negative control), which is exactly what the entry asks. Three instances are exhibited (`Willard2005` §6 `NS^{k,m}_D`, `Willard2007-APAL` p. 29, `Willard2016` Rem. 7.4); the 1993 INVALID.1/INVALID.2 instance is not, and D16 is cited by id nowhere — cosmetic, since D29 carries the citation at that point. |
| D17 | open — the codified statement should state the convention once | **remains-open — finding** | **Open because** the convention is nowhere stated. **Both readings: not presented.** `metatheor`, `meta-theor` and `Meta-Logic` all return zero hits. §4.3 disambiguates *Definable Cut* from *cut rule*, which is a different collision (D32's neighbourhood), and does not say that the object systems are cut-free while the reasoning about them uses cut freely — TR Remark 2's "we will often omit constructing formally the cut-free proofs … their theoretical existence will be established implicitly". O19 undischarged; this qualifies the proof-status of every 1993 result the document restates. |
| D18 | open | **remains-open — finding** | Same failure as D7, from the 2011 side: `Willard2011` §1's *Self Justifying* **is** 2005's *Introspectively Unified Logic*, and 2005's *Self-Justifying* has no 2011 name. **Both readings: not presented.** §2.2 states only `Willard2016` Def. 3.4. D18 uncited. |
| D19 | open | **remains-open — finding (partial)** | **Open because** the three formulations of Solovay's theorem are not shown to be non-interchangeable. **Both readings: partially presented.** §2.4 names its chosen formulation (`Willard2006a` pp. 4–5 Thm 2, Hilbert deduction, successor totality plus Π₁ associativity/commutativity/distributivity/identity) and gives `Willard1993-TR` p. 2's reading of it. Absent: the TR p. 10 form (Gentzen-style **sequent calculus with cuts**, naming Subtraction, non-zero Division and Successor) and `Willard2011` Thm 2.1's **Type-S** form. §8's Type-S row cites Solovay `∗` without saying which formulation closes that cell. |
| D20 | explained:2026-08-21 | resolved | `Willard2001`'s title corrected in `registry/corpus.md` and `nachlass/paperlist` at C6. codified-sjas.md cites no paper titles, so nothing to present. |
| D21 | open (full analysis in `../extraction/willard2011-subsumption-audit.md` §2.2) | resolved | The entry's requirement is a **prohibition** — "must not present a four-to-four correspondence" — and codified-sjas.md complies: only `ξ^R` of the four configurations appears (§3.1, §8), correctly at Type-Almost-M, and the four *paradigms* are never enumerated against them. Compliance is by omission rather than by exposition, which is sufficient for the prohibition. |
| D22 | open (gap G15) | resolved | The entry's operative rule is "cite section and item labels; never JSL page numbers from this witness." Verified: all thirteen `Willard2001` citations in codified-sjas.md are by section, definition, theorem or remark number (§1.4 §1; §2.4 App. A; §5.5 Thm 3.4; §6.2 Def. 1.1; §6.9 Thms 3.4, 4.3; §7.2 Rem. 4.5; §9.4 Rem. 3.8). Zero page citations. G15 stays open at the registry level, not against this document. |
| D23 | **resolved** — presentational, not substantive | resolved | §3.2 presents the six/seven/eight split as a table with sources and states the real constraint (Non-Growth plus finite Π⁻₁ axiomatisability), which is the entry's own recommendation. **Note:** the entry keys its seven-function list to `Willard2001` p. 9; §3.2's table attributes seven to `Willard2006a` (correct — image-verified at `../extraction/willard2006a.md` p. 6) and omits `Willard2001` entirely, so the specific pair D23 compares is not visible. |
| D24 | open — C13 must state one admission condition and tabulate the rest | **remains-open — finding** | **Open because** no admission condition is stated and none of the three is tabulated. **Both readings: not presented.** `Regularly Consistent` returns zero hits; the phrase occurs once, lower-case and undefined, in §6.9's table cell "`A` regularly consistent ⟹ `IS(A)` consistent". `Willard2001`'s conjunction (Δ⁻₀ `Prf_A` encoding **plus** standard-model validity), `Willard2005`'s Π\*₁-true and `Willard2011`'s Σ^ξ₁-complete are nowhere compared, and the entry's warning that "regularly consistent" names a **soundness** condition — misleading in isolation — is exactly the misreading §6.9 now invites. Not to be confused with D55, which §6.1 does discharge: that entry is about *consistency preservation*, this one about the *admission condition on the base*. |
| D25 | **resolved on sight** | resolved | The four apparently incompatible density constants are one scheme. §6.6 presents 1/5 with Appendix A's six-bit byte as its source. **Note for R5:** obligation O33 asks for the bits-versus-payload distinction (five base-32 payload bits in a six-bit byte, hence 1993's `6/5`) to be stated **once, explicitly**; §6.6 juxtaposes the 6 and the 5 without stating the relation, and §9.2's "at six bits per symbol" repeats the 6 alone. The drift is closed; the obligation is not. |
| D26 | open — C13 must present the Level-0/Level-1 distinction as a design axis | **remains-open — finding** | **Open because** the axis is absent. **Both readings: not presented.** §5.2 presents the *consistency-level* axis (Level(0−)/Level(1)/Level(n)/Level(n+)), which is a **different** axis from D26's: whether the size threshold is a clause the system itself quantifies over (`IS^λ(A)`, `ISREF(A)`, `ISTR(A)`, `ISM(A)`) or a condition on the metatheory (`IS(A)`, `Willard2005`, `Willard2011`). Worse, D26 labels its own distinction "Level-0/Level-1", so a reader moving between the ledger and §5.2 meets the same words for two different things. The corrected phrasing is at least not repeated: codified-sjas.md nowhere says the threshold "lives entirely in the metatheory". |
| D27 | open (action assigned to C9) | remains-open (mild) | **Open because** the lineage is unstated. **Both readings: not presented.** `Willard2020` and `Willard2020-LFCS` are cited independently (§3.2, §4.4, §5.3, §8) with no statement that they are one paper at two lengths, and the terminal chain `Willard2018` → LFCS → arXiv → `Willard2021` appears nowhere. No misstatement results, but D37 makes the relation load-bearing rather than bibliographic. |
| D28 | **resolved** | resolved | The 2020 draft review is a first-party artifact of this repository, recorded so the provenance apparatus can cite it rather than treat it as an untraced loose file, and so G19 closes as accepted. codified-sjas.md does not cite it; no presentation obligation. |
| D29 | **explained:2026-08-31** | **resolved — verified** | The deficiency-11 precedent, checked in full. (i) §3.1 defines Type-Almost-M by the **axiom-versus-theorem** criterion, not as an intermediate totality strength, and quotes Willard's "many axiom systems that use function symbols '+' and '\*' fall technically into" it. (ii) The indexing question the entry left for the codification is **answered**: "This codification adopts the four-class lattice of `Willard2016` and `Willard2020` as Axis 1, and places Type-Almost-M **beside** it … as a second and independent coordinate." (iii) Both consequences landed: `Willard2011`'s `ξ^R` is out of the Type-A row and quoted from Appendix D, and `Ax-3` joins it — confirmed in codified-sjas.md §8 *and* in `result-matrix.md` line 23, whose Type-A row (line 22) no longer carries `ξ^R`. (iv) The tie to §7.4 is drawn: Herbrand-but-not-tableaux verification is stated to be one phenomenon. (v) D29 is cited by id at §3.1 and §5.4. Both indexings remain on record in the ledger, as the status line says. **No residue.** |
| D30 | open — C13 should present one construction with its two instantiations | **remains-open — cited, half-discharged** | **Both readings: partially presented.** §4.5 cites drift **D30**, names the Linear-Sum Effect once, and gives its three aliases (*Tableaux Δ₀ Compression*, *Hyper-Constructivity*, `Willard2007-APAL` App. B's `∗`). **Shortfall:** the entry's actual claim — that `Willard2002a` Lemma 6.3 is the same construction eighteen years early, driven by an *implication* axiom where 2020 uses *LEM* — is not presented; §4.5's device table carries the 2020 instantiation only, and the five-fold recurrence the entry lists is left to `replication-map.md`. This is the deficiency-10 shape: cited at the point of use without doing what it says. |
| D31 | open — C13 must name the three conditions distinctly | **remains-open — finding** | **Open because** the three conditions are not named distinctly. **Both readings: not presented, and the entry is actively contradicted.** "Conventional Deciphering Property" occurs three times, always unqualified: twice in §7.1 for `Willard2002a` Thms 5.2 and 6.4 (Def. 5.1's *finite-subset* condition) and once in §7.5 for `Willard2004` Remark 1 (the `Σ*₀` `Test(n)` *definability* condition). D31 states these are inequivalent. `Willard2006a`'s third notion, **Concise Encoding**, is absent. D31 uncited. |
| D32 | open (C13) | **remains-open — finding** | **Open because** no definition is adopted on the record. **Both readings: not presented.** §4.3 gives exactly one Definable Cut definition — `φ(0)`, successor closure, downward closure, i.e. `Willard2006a` Eq. (4) — without naming its source and without noting that `Willard2001` p. 5 replaces `φ(0)` with a schema over all fixed numerals, that `Willard2002a` Eq. (1) omits downward closure, or that `Willard2020-LFCS` Def. 4 replaces successor closure with closure under the fast-growing `ω_j`. The last is not academic: it is the form that reaches the Pudlák / Wilkie–Paris cut inside `L*`, which is §9.1's subject. The entry's dependent claim (O29, tangibility as a definable cut minus successor closure) cannot be checked because tangibility is absent from the document altogether — see D79. |
| D33 | **resolved** — one apparatus, four names | resolved | §4.5's restricted-cut row lists `Tab-ℜ-List`, `Tab₁List`, `Tab-1`, `Herb−k` and `R(i,j)` together as one apparatus family, and §8's matrix column fixes "TabList". The entry's second limb — `Willard2009` p. 20's gloss that the starred classes are "roughly analogous" to the R-superscripted ones except for the multiplication function symbol — is carried verbatim at §3.3. |
| D34 | open — appended to gap G22 and obligation O46 | resolved | §8.1 states the prohibition and the reason: "Two of these must **not** be merged. `Willard2004`'s `ℜ` parameterises which *intermediate theorems* a TabList proof may use; `Willard2020`'s `Z` parameterises which *LEM instances* are logical axioms … (drift **D34**, obligation **O46**)". §9.1 then uses the distinction to state the open problem. Both readings present; the G35 correction has landed on both sides (§9.1 says "affirmative but only `sketch`", matching the amended entry). Ledger status is stale; superseded by D38 in any case. |
| D35 | open (C13) | remains-open | **Open because** the entry asks for one condition all four papers' function sets satisfy, and none is given. **Both readings: presented** at §3.2 — the standard `F(a₁,…,a_j) ≤ Max(a₁,…,a_j)` in the body, and "`Willard2006a` writes `≤ Maximum(2, a₁,…,a_j)`" flagged as recorded-rather-than-resolved, with drift **D35** cited by id. Correct handling of a live divergence. |
| D36 | open — closes the C6-deferred `Willard2006a` subsumption check | resolved | The risk the entry guards against is a claim that `Willard2011` subsumes `Willard2006a`; codified-sjas.md makes no such claim, and independently carries the dropped **naming-convention axis** at §5.5 (incremental / additive / Hybrid(H) / multiplicative, with verdicts) and **Continuously Expanding** at §5.3 and §5.5. **Note:** `ISINF` / *Infinitely Far-Reaching* returns zero hits and is carried nowhere. |
| D37 | open — extracted as a first-class record, `willard2020-lfcs.md` | **remains-open — finding** | **Open because** the LFCS-only material is not carried. **Both readings: not presented.** `Locally-J` returns zero hits; Definition 4, Theorem 3 and the `ω_j` hierarchy appear nowhere. This matters more than a bibliographic omission: by the entry's own reading, Appendix B is the **only** place in the corpus explaining how `Willard2020`'s ⊙ reaches the Hájek–Pudlák definable-cut machinery — LEM → Linear-Sum Effect → short proofs of the Locally-J-Closed conditions → cut diagonalization — which is precisely the route §9.1's `Π*₁` conjecture would have to travel. D37 uncited; the entry's support for G1's priority is likewise unstated. |
| D38 | open (C13). Supersedes the narrower D34 | resolved | §8.1 does exactly what the entry asks: states the boundary once as a single phenomenon, then coordinatises it in a five-row table (`ℜ`, Level(n), Hybrid(H), `Herb−k`, `Z`) with **positive**, **negative** and a status column for each half, cites drift **D38** and obligations O49/O60, and §8.2 reads the statuses back honestly ("both exactly-located dials are `stated-only` throughout"). Ledger status is stale. See §4 finding **F-L2** on the entry's own stale heading. |
| D39 | open (C13) | resolved | §9.1: "`Willard2004`'s `W₄` and `W₅` (Eqs. 12–13) … are already Z-enrichment axioms in `Willard2020`'s sense, for the two `Σ*₀` predicates `Prf` and `Subst`, and Lemma 5's proof uses them exactly to generate the `∨`-elimination splits its case analysis needs", with the sixteen-year gap stated and drift **D39** cited. Both readings present. Ledger status stale. |
| D40 | open (C13) | **remains-open — finding** | **Open because** the two-toolkit organisation is absent. **Both readings: not presented.** `Generalized Gödel` and `ShortPrf` both return zero hits — the cut-free-side toolkit (`⧫^λ_D(α)` over `ShortPrf^λ_{α,D}`) is never introduced at all, so `Willard2002a`'s and `Willard2004`'s apparatus appears ad hoc, which is the outcome the entry exists to prevent. §4.3 introduces definable cuts and adds `Willard2002c` Remark 6 (Willard's systems are not cut-localized), but not `Willard2006a` p. 12's reason for the split: definable cuts "are not always ideally suited … when D represents a **cut-free** deduction method". |
| D41 | open (C13) | remains-open — finding (mild) | **Open because** the Hilbert-side disclaimer is not recorded. **Both readings: not presented.** `uncompressed` / `non-compressed` return zero hits. §6.6 presents coding density as a hypothesis of the tableaux engine without scoping the claim, and `Willard2006a` pp. 15 and 37 — results "also generalize in various forms for non-compressed encodings", which are "substantially more meaningful" rather than *valid* only when compressed — are absent. Partly mitigated: §6.9 says the Hilbert line does not use θ-Compactification at all, so a careful reader will not over-extend §6.6. |
| D42 | open — C13 should present `Xtab` with its 1993/94 provenance | **remains-open — finding (highest severity)** | **Open because** the provenance is not presented **and the retracted reading is restated.** §7.3 closes with: "The identification of LEM-as-axioms with cuts is already in `Willard1993-TR`; obligation **O55**." That is the claim D42's **2026-08-27 correction explicitly withdrew** — the correction states the TR "does not" assert the identification in general, that printed p. 36 is a hedged parenthetical about ZF alone ("*can be viewed as* performing deduction with Gentzen-style cuts"), and that the definitional content is entirely p. 33's *naming* of an apparatus. This is deficiency **4** in its exact shape: the ledger entry was amended, the dependent sentence was not re-read. Separately, §4.5's `Xtab` row attributes the device to `Willard2020` App. with no 1993/94 provenance, and D42 is cited nowhere. **Secondary, for R2:** D42 anchors `Xtab`'s definition at `Willard2020` **p. 11**, while §4.5 cites "`Willard2020` App."; the anchor should be reconciled. |
| D43 | open — cross-referenced from G22 and obligation O48 | **remains-open — finding** | **Open because** the TR's justification is not carried. **Both readings: not presented.** §4.5's "LEM at `Δ₀` only" row cites `Willard2007-APAL` Lem. 1(i) — a *different* source for a similar restriction — and neither §8.1 nor §9.1 mentions `Willard1993-TR` printed p. 35's footnote, which supplies the argument `Willard2020` §7 declines to give: those LEM instances are already derivable through Group-2 at polynomial cost, so promoting them to axioms yields no additional proof compression. Consequence: §8.1 marks the `Z` dial's `Δ*₀` positive `stated-only` and §9.1 presents the `Δ*₀` flank as merely asserted, both of which **understate** what the corpus contains. D43 uncited. |
| D44 | open (C13) | resolved | §2.4 presents exactly this derivation: `Willard1993-TR` p. 2 read as forbidding a cut-permitting apparatus and Addition-totality *together*, then the two-row branch table (tableaux line = Proposition 1 relaxes the apparatus; Hilbert line = Proposition 7 relaxes totality), with the 2016 restatement alongside. §3.1 and §8.2 refer back to it as the corpus's organising fact. Cited there as D49 rather than D44 — cosmetic. Ledger status stale. |
| D45 | open (C13) | remains-open | **Open because** the entry asks C13 to fix **one** operator and restate each paper's lemmas against it; codified-sjas.md fixes `Willard2005`'s `App∀`/`App∃` for its own engine chapter (§6.3–§6.5) but does not restate the other papers' lemmas against it. **Both readings: presented** — §6.2's five-row table gives all five forms (`Φ^i_j`, the `(L,M)`-Conservative Branch, `G-good`, `App∀`/`App∃`, `Scope_E`) with distinguishing notes. **Shortfall:** the entry's load-bearing point — `Willard2002c` truncates **universals only**, which is what lets `Check` compare `∀a φ(a)` against `∃a ¬φ(a)` asymmetrically — is not the one §6.2's row records. D45 uncited; O27 is discharged at §6.5 on the negation side. |
| D46 | open (C13) | resolved | §8.1 presents the two-index form in full: "`Willard2002c`'s **two-index `R(i,j)`**, where `R(1,1)` works and **`R(2,1)` fails**. The `ℜ` dial of `Willard2004` is a *projection* of `R(i,j)` onto one index (drift **D46**), so the two-index form carries strictly more information and is the sharpest localisation in the corpus. … Obligation **O60**." This is the repair of deficiency **10**'s named instance (§8.1 previously cited O60 while never presenting `R(i,j)`'s two-index form). Verified discharged. |
| D47 | open (C13) | resolved | §7.4: the conference witness's sharpest claim is carried — "`Willard2006-WoLLIC` p. 10 states that `Diag(1)` and `Diag(2)` are **provably logically invalid** while `Diag(3)` is valid … The journal version drops it (drift **D47**, obligation **O69**)" — with the non-superset relation stated. Both readings present. Ledger status stale. |
| D48 | closed (dependency extracted; Theorem 5 status unchanged) | resolved | `Willard2007-APAL` extracted at full depth (G32 closed, O70 discharged). Consumed correctly downstream: §4.4 fixes its Definition 1's **eight** rules as canonical *because* `Willard2009` Appendix B invokes them by number, and §4.5 records Passive Induction as what that appendix depends on. `Willard2009` Thm 5 is still labelled `sketch` at §7.4 and §7.6, as the status line requires. |
| D49 | open (C13) | resolved | §2.4 records both namings and their non-relation: the branch table's "1993 derivation" and "2016 restatement" columns, then "`Willard2016` Example 3.3(b) restates the same split twenty-three years later in the Type vocabulary … **without mentioning the 1993 derivation**. Obligations **O57**, **O71**; drift **D49**." The entry's third datum (Type-M systems cannot recognize tableaux consistency) is carried in §8's Type-M row and §7.4's Adamowicz–Zbierski attribution. Ledger status stale. |
| D50 | open (C13) | remains-open | **Open because** no canonical class notation is adopted and no translation table is given — correctly, since the entry establishes that only two inter-class relations exist in print. **Both readings: presented** at §3.3, whose seven-row table carries every family (including `Δ⁻₀`, which the ledger's own table omits) and which states both known relations verbatim with their anchors (`Willard2009` p. 20; footnote 3, p. 13) and says every other pair is unrelated in the literature. Drift **D50** cited. See §4 finding **F-L3** on the unsubstantiated count "ten". |
| D51 | open (C13) | carried | `Q₀`'s eight-sentence (`Willard2007-APAL`) versus nine-sentence (`Willard2009`, `Willard2006-WoLLIC`) readings. Carried: codified-sjas.md never writes `Q₀` — the single `Q₀`-looking hit at §3.3 is `Δ^Q₀`, an unrelated formula class — so the silent-wrong-axiom-set failure the entry predicts cannot occur here. Belongs to Refinement when the passive-induction and `Ax-3` lines are stated at axiom grain. |
| D53 | open (C13) | remains-open | **Open because** no canonical rounding is chosen, and the entry says one must be. **Both readings: presented** at §3.2: "`Root(x,y)` rounds **up** in `Willard1998`, `Willard2002c`, `Willard2004` and `Willard2005` but **down** in `Willard2001` and `Willard2016` (drifts **D53**, **D80**). The corpus is split, not one outlier against a consensus, and a codified grounding language must choose." Correct handling. See §4 finding **F-C2** on the papers missing from that split. |
| D54 | open (C13) | resolved | §4.4 presents both readings with anchors — eight primitive rules (`Willard2007-APAL` Def. 1, `Willard2005` §2) versus six with bounded quantifiers as abbreviations (`Willard2020` App., `Willard2020-LFCS` App. A, `Willard2002c` §2), plus `Willard2002a`'s six with simultaneous multi-variable `∀`-elimination — cites drift **D54**, **fixes** the eight-rule presentation as canonical, and gives the reason (`Willard2009` App. B invokes them by number). Also carries `Willard2002c`'s non-presentational extra restriction on `∀`-elimination terms. **Residual for R5:** the entry also asks for the numbering *map* between the presentations; §4.4 gives the reason a map is needed but not the map (O42). |
| D55 | open (C13); the codified statement must name the three hypotheses separately | resolved | §6.1 tabulates all three hypotheses with sources, states the direction of the only available comparison ("`Willard2005`'s **hypothesis** is the weakest of the three … so as a *property of the map* it is correspondingly the **strongest**: 2020-preservation follows from 2005-preservation, not the reverse"), records that `Willard2016`'s is **incomparable** with both and why, notes all three head a headline theorem, and cites drift **D55** with the rule "the phrase is never to be written unqualified". Direction of the comparison checked against the entry — consistent. Ledger status stale. |
| D56 | open (C13) — fix one name | remains-open | **Open because** the entry's operative instruction is to fix one name, and none is fixed. **Both readings: presented** — §6.2's closing paragraph lists all four (the conservative branch, the **Partial Proof** with its **Open Branch** and **Bottom Node**, the **Semantic Tableaux Fragment** with its **Major Branch**, the **pivotal branch**) with sources, and cites drift **D56**. The entry's point that this construct is where the `u_{i+1} = u_i·u_i` ladder is laid down is carried at §7.2. |
| D57 | explained:2026-08-29 | resolved | One-digit slip (1189–**1999** for 1189–1199) in `Willard2005-TAB`'s forward citation, image-confirmed. No codified presentation obligation. See §4 finding **F-L1**: this entry and **D72** record the same fact under two ids. |
| D70 | open (C13). Amended 2026-08-29 | carried | The `Willard2005-TAB` / `Willard2006b` correspondence table the amendment says can now be composed is not in codified-sjas.md. Carried rather than flagged, because §8.2 **scopes the line out with a stated reason**: "the real-valued line (`Willard2005-TAB`, `Willard2006b`) sits outside the integer grid entirely, since `IS_D(A′)` recognises multiplication as total **over simulated reals** — its primary witness, the ASL-2005 technical report, is unlocated (**G36**)." The mis-keying risk the entry names is therefore confined to the registries and extraction records, where the table belongs. |
| D71 | explained:2026-08-29 (image governs) | resolved | Def. 3's simulated-real value formula is destroyed by `pdftotext` in both witnesses. §10.3's hazard **H2** records the corruption class ("dropped numeral overbars, tower super/subscripts and **floor/ceiling delimiters**") and states the governing rule unconditionally: every formula read from a rendered page image. |
| D72 | explained:2026-08-29 | resolved | Same disposition as D57 — and same content; see finding **F-L1**. |
| D73 | open (C13 presentation) | carried | Definition numbering restarts inside `Willard2005-TAB` (the p. 6 unnumbered "1."/"2." Level pair versus pp. 7–10's Definitions 1–6). A registry-keying concern for a witness whose line is scoped out of codified-sjas.md at §8.2; no collision can arise in this document. Carried to the registry layer. |
| D74 | explained:2026-08-29 (statement governs) | resolved | `Π₁⁻` in Theorem 4's statement, `Π₁*` once in its proof sketch; the statement governs because the paper's own §3 convention and Theorem 4's Grounding-language `α` agree with `Π₁⁻`. `Willard2005-TAB` Theorem 4 is not presented in codified-sjas.md. |
| D75 | open (C13); pairs with D5 | resolved | The entry's instruction is to use `IS_D(A)` and treat `ISD` / `IS-1*` as orthographic variants. codified-sjas.md complies without exception: `IS_D` throughout §5.1, §5.4, §6.4–§6.9; neither `ISD` nor `IS-1*` occurs. The paired collision, **D5**, is a separate finding above. |
| D76 | explained:2026-08-29 | resolved | §5.6 keeps the finite and infinite systems distinct exactly as directed: `Willard2014` §§5–6's **`IS_D^#(β)`** is presented as the finite-Group-2 variant with its price stated, and `Willard2016` Remark 7.3 is presented separately as a `cited`, not-proved generalization claim — never as a third system. G33 closed, O73 discharged. |
| D77 | explained:2026-08-29 (C12) | resolved | The Hájek packet is a third-party letter, not Willard's mathematics, and codified-sjas.md does not cite it as mathematics. **Minor note:** §9.4 lists "the Hájek correspondence" among "nine witness-grade records from Willard's papers" without marking the authorship; a clarifying clause would remove the residual reading the entry warns against. |
| D78 | open (C13 / G1 acquisition) | resolved | §9.4 presents the 2025 packets as the entry directs — "The 2025 drafts carry **title-page and referee witnesses** for `Willard2021` (obligation **O95**)" — while G1 stays listed as "unacquired; the terminal paper". That is precisely "pipeline witnesses only; G1 still needs the published JLC text". Ledger status stale. |
| D79 | open (C13 must choose a canonical TangPred or record the delta) | **remains-open — finding** | **Open because** neither printing is chosen and the delta is not recorded in the codified statement. **Both readings: not presented.** `Tangib` and `TangPred` both return zero hits in codified-sjas.md — the tangibility apparatus is absent from the document entirely, so `Willard1998` p. 300's `∃z x = z − 1` and `Willard2001` Def. 1.2's `∃v x < v − 1` are neither compared nor chosen between. The absence also leaves O28 (the three indexed tangibility predicates) and O29 (tangibility as a definable cut minus successor closure) with nowhere to land, and it is why D32's dependent claim cannot be checked. |
| D80 | open (note at C13; low urgency) | remains-open | **Open because** no canonical rounding is chosen. **Both readings: presented** for the `Root` limb at §3.2, with drift **D80** cited alongside D53. **Shortfall:** the entry's two other limbs are not carried — `Willard1998`'s non-growth bound `≤ Maximum(2, a₁,…,a_j)` (§3.2 attributes that variant to `Willard2006a` alone, under D35) and its eight-function list including `Bit` against `Willard2001`'s seven (neither paper appears in §3.2's function table). See finding **F-C2**. |

---

## 3. Totals

**Entries walked: 67** (D1–D51, D53–D57, D70–D80). The disposition table has 68
rows: `D1-supplement` gets its own row for legibility but is not a numbered
entry and is excluded from every count below.

### By prior status

| Prior status in the ledger | Count | Ids |
| --- | --- | --- |
| `open` (incl. `open (C13)`, `open (Cn)`, `open — …`) | **52** | all not listed below |
| `explained:YYYY-MM-DD` | 10 | D1, D4, D20 (08-21); D57, D71, D72, D74, D76, D77 (08-29); **D29** (08-31) |
| `resolved` / `resolved on sight` | 4 | D23, D25, D28, D33 |
| `closed` | 1 | D48 |
| **Total** | **67** | |

### By disposition

| Disposition | Count | Ids |
| --- | --- | --- |
| **resolved** | **31** | D1, D4, D6, D16, D20, D21, D22, D23, D25, D28, **D29**, D33, D34, D36, D38, D39, D44, D46, D47, D48, D49, D54, D55, D57, D71, D72, D74, D75, D76, D77, D78 |
| **remains-open** | **31** | D3, D5, D7, D8, D9, D10, D11, D12, D13, D15, D17, D18, D19, D24, D26, D27, D30, D31, D32, D35, D37, D40, D41, D42, D43, D45, D50, D53, D56, D79, D80 |
| **carried** | **5** | D2, D14, D51, D70, D73 |
| **Total** | **67** | |

Of the 31 `remains-open`, **six** are the healthy case — the divergence is live,
the codified statement presents both readings with anchors, and only the
canonical choice is deferred: **D3, D11, D35, D45, D53, D56**. The other **25**
carry a finding, listed in §4.1 and §4.2.

### Coverage by the codified statement

`codified-sjas.md` cites **16** drift ids: D6, D29, D30, D34, D35, D38, D39,
D46, D47, D49, D50, D53, D54, D55, D56, D80. Fifteen are `open` in the ledger;
D29 is `explained`.

| Of the 15 `open` ids cited | Count | Ids |
| --- | --- | --- |
| Fully discharged — both readings presented, or one fixed with a reason | 9 | D6, D34, D38, D39, D46, D47, D49, D54, D55 |
| Both readings presented, canonical choice deferred | 3 | D35, D53, D56 |
| Cited but only half-discharged, or with a count defect | 3 | D30, D50, D80 (§4.1) |

**Thirty-seven `open` entries are cited nowhere in `codified-sjas.md`.** Five
of those are carried with reasons (D2, D14, D51, D70, D73); the twenty-two ids
behind findings **F1–F21** in §4.2 are the rest of the consequential set.

---

## 4. Findings

### 4.1 Open drift cited in `codified-sjas.md` where both readings are NOT presented

This is R6's headline question. Of the 15 `open` ids the document cites, three
fall short.

| Id | Cited at | What is missing |
| --- | --- | --- |
| **D30** | §4.5 | The entry's own claim — `Willard2002a` Lemma 6.3 is the same construction eighteen years early, driven by an *implication* axiom where `Willard2020` §6 uses *LEM* — is not presented. §4.5's device table gives the 2020 instantiation only, so "one construction with its two instantiations" is half-done. Deficiency **10**. |
| **D50** | §3.3 | The families and both in-print relations are presented, but §3.3 asserts the corpus contains "**ten**" relativisations while tabulating seven — and the ledger entry's own table lists six. Neither artifact substantiates the count. Deficiency **9**. |
| **D80** | §3.2 | The `Root` rounding split is presented; the entry's other two limbs are not. `Willard1998`'s non-growth bound `≤ Maximum(2, …)` is attributed at §3.2 to `Willard2006a` alone, and neither `Willard1998`'s eight-function list (with `Bit`) nor `Willard2001`'s seven appears in the function table. |

### 4.2 Open drift NOT cited, where `codified-sjas.md` makes a claim that needs it

More consequential than §4.1, and the same failure mode as deficiency **11**:
twenty-one findings over twenty-two drift ids, ordered by severity.

| # | Id | Where | Finding |
| --- | --- | --- | --- |
| **F1** | **D42** | §7.3 | **codified-sjas.md restates a claim the drift ledger retracted.** §7.3: "The identification of LEM-as-axioms with cuts is already in `Willard1993-TR`". D42's 2026-08-27 correction says the TR does **not** state that identification — printed p. 36 is a hedged parenthetical about ZF alone, and the definitional content is p. 33's naming of an apparatus. Deficiency **4** exactly: entry amended, dependent sentence never re-read. Fix: rephrase to the definitional reading and anchor it to TR printed p. 33, citing D42. |
| **F2** | **D31** | §7.1 ×2, §7.5 | "Conventional Deciphering Property" is used **unqualified three times** across `Willard2002a` (finite-subset recognition, Def. 5.1) and `Willard2004` (`Σ*₀` `Test(n)` definability, Rem. 1), which D31 records as inequivalent. `Willard2006a`'s **Concise Encoding** is absent. A reader takes three hypotheses for one. |
| **F3** | **D24** | §6.9 | The admission condition on the base system is never stated or tabulated. §6.9's table cell reads "`A` regularly consistent" with no definition, and the entry warns that the phrase names a **soundness** condition, misleading in isolation. `Willard2001`'s Δ⁻₀-`Prf_A` encoding requirement, absent from `Willard2005`'s and `Willard2011`'s purely semantic conditions, is invisible. Distinct from D55, which §6.1 does discharge. |
| **F4** | **D7 + D18** | §2.2 | The corpus's **three** closely-named, non-equivalent predicates are never tabulated (O25). §2.2 fixes one vocabulary, which is half the instruction; 1993's *self-verifying*, 2005's *Introspectively Unified Logic* and 2005's ∃d *Self-Justifying* return zero hits. This is D29's failure class — an entry whose whole content is "fix one and tabulate the rest", with only the first half done. |
| **F5** | **D79** | *(absent)* | The **tangibility apparatus is absent from codified-sjas.md entirely** (`Tangib`, `TangPred`: zero hits), so `Willard1998` p. 300's `∃z x = z − 1` and `Willard2001` Def. 1.2's `∃v x < v − 1` are neither compared nor chosen between. Knock-on: O28 and O29 have nowhere to land, and D32's dependent claim is unverifiable. |
| **F6** | **D37** | §9.1 | The LFCS-only material — Def. 4 (Locally-J-Closed), Theorem 3, the `ω_j` hierarchy — is not carried (`Locally-J`: zero hits). By D37's own reading this is the **only** corpus explanation of how `Willard2020`'s ⊙ reaches the Hájek–Pudlák machinery, i.e. the route §9.1's conjecture must travel. |
| **F7** | **D43** | §8.1, §9.1 | `Willard1993-TR` p. 35's footnote supplies the argument `Willard2020` §7 declines to give for `Δ*₀` (Group-2 already yields those LEM instances at polynomial cost, hence no added compression). §8.1 marks the `Z` dial's `Δ*₀` positive `stated-only` and §9.1 presents it as merely asserted. The document **understates** its own corpus. |
| **F8** | **D40** | §4.3 | The cut-free-side toolkit is never introduced: `Generalized Gödel`, `ShortPrf` both zero hits. `Willard2006a` p. 12's reason for the two-toolkit split is absent, so `Willard2002a`'s and `Willard2004`'s apparatus reads as ad hoc — the outcome the entry exists to prevent. |
| **F9** | **D15** | §6.7 vs §7.2 | Only one of the two routes to the multiplication boundary is given. §6.7 has 2005 Remark 4's envelope argument in full; `Willard1993-TR` §12's reflection-derivability route (Lemma 12.1 upgrades bounded to full local Π₁ reflection, which Proposition 5 has shown inconsistent) is absent. O14 requires both presented as two routes to one boundary. |
| **F10** | **D26** | §5.2 | The size-threshold-location axis (in-axiom versus metatheoretic) is absent; §5.2 presents only the consistency-level axis. Compounded by a vocabulary collision: D26 calls its own distinction "Level-0/Level-1", the same words §5.2 uses for a different axis. |
| **F11** | **D32** | §4.3 | One Definable Cut definition is given (`Willard2006a` Eq. 4's three clauses) with no source named and no variants noted. The omission of `Willard2020-LFCS` Def. 4's `ω_j` closure is not academic: that is the form reaching the Pudlák / Wilkie–Paris cut inside `L*`, which §9.1 is about. |
| **F12** | **D17** | *(absent)* | The Meta-Logic convention — cut-free object systems reasoned about with cut freely available (TR Remark 2) — is nowhere stated (`metatheor`, `Meta-Logic`: zero hits). O19 undischarged. This qualifies the proof status of every 1993 result the document restates. |
| **F13** | **D5** | §5.1, §5.2, §6.9 | The `IS(A)` / `IS_D(A)` delta is never stated in one place. The names are kept distinct and the Level difference is inferable across three sections, but O7's "state the delta once" is undone. Pairs with D75, which *is* clean. |
| **F14** | **D9** | §3.3 | `Willard1993` p. 327's majorization-style Π₁ condition is absent, so the TR-versus-chapter divergence is invisible; §3.3 handles only the notational half of O12. |
| **F15** | **D12** | §3.2, §6.3 | The Π₂ → Π\*₁ consequence — the load-bearing part by the entry's own words — is never drawn, although §3.2 states the premise and §6.3 consumes the conclusion. O11 undischarged. |
| **F16** | **D13** | §7.2 | §7.2 cites `Willard1993` pp. 328–331 but does not present the *duplication* framing or its link to Gödel's diagonalization, which is the whole of O9. (The affine-tree documents themselves are out of scope by the entry's own last bullet.) |
| **F17** | **D19** | §2.4, §8 | `Willard2011` Thm 2.1's Type-S formulation and the TR p. 10 sequent-calculus-with-cuts formulation are absent; §2.4 presents `Willard2006a` Thm 2 and the TR p. 2 reading only. §8's Type-S row cites Solovay `∗` without saying which formulation closes the cell. |
| **F18** | **D41** | §6.6 | `Willard2006a`'s explicit disclaimer that its Hilbert-side results hold for non-compressed encodings (compression affects *significance*, not validity) is absent, so §6.6's density hypothesis reads as universal. Mitigated by §6.9. |
| **F19** | **D27** | §3.2, §4.4, §5.3 | The `Willard2018` → LFCS → arXiv → `Willard2021` lineage is unstated; the two 2020 witnesses are cited as independent papers. Mild alone, load-bearing given F6. |
| **F20** | **D8** | §9.3 | §9.3 writes `IS*_+(A)` — precisely the star that D8 warns collides with the *formula-class* stars of §3.3 — with no note, and the `IS*(A)` / `IS^{Σ₁}(A)` divergence between the two 1993 witnesses is not recorded. |
| **F21** | **D10** | §3.2 | Neither 1993 function list appears (the chapter's fourteen, the TR's eight), so the fourteen-to-eight simplification is invisible. Mild: the entry's *relativizer* — growth class, not signature, is the invariant — is carried at §3.3 with O13, which is the substance. |

### 4.3 Defects in the drift ledger itself

| # | Finding |
| --- | --- |
| **F-L1** | **D57 and D72 are near-duplicate entries** — the same `Willard2005-TAB` JSL page-range typo (1189–1999 for 1189–1199), both `explained:2026-08-29`, both image-verified, recorded under two ids. D70 links to D57; the registry links to D72. Deficiency **8**, in the drift ledger rather than in `results.md`, and outside what `audit.sh` M2 covers. Merge, or make one a pointer. |
| **F-L2** | **D38's heading is stale relative to its twice-amended body.** The heading reads "the boundary has **three** dials, and **only one** has its threshold located"; the 2026-08-27 amendment retracts the "only one", and the 2026-08-28 amendment brings the count to five ("the weakest-supported of the **five**"). §8.1 correctly says five. Deficiency **9** / **4**. |
| **F-L3** | **D50's "ten formula-class notations" is unsubstantiated.** The entry's table has six rows; §3.3's has seven. Neither reaches ten, and no derivation of the count is given. Deficiency **9**. |
| **F-L4** | **Sixteen entries carry stale `open` statuses.** D6, D16, D21, D22, D34, D36, D38, D39, D44, D46, D47, D49, D54, D55, D75 and D78 are dispositioned `resolved` above because the C15 draft discharges them, but every one still reads `open` or `open (C13)`. C13–C15 never wrote back. This is deficiency **4** running the other way — the ledger going stale against the document rather than the document against the ledger — and it is why an R6 walk was needed to discover that D29's sibling entries were quietly finished. |
| **F-L5** | **Heading order is non-numeric** (D1, D7–D14, D2–D6, D15–D48, D51, D49, D1-supplement, D50, D53–D57, D70–D80). Combined with the unused 52–69 band, a reader cannot tell a deliberate gap from a misfiled entry without the sweep in §1. |
| **F-L6** | **D2's assigned action is undischarged.** "Confirm during the C9 extraction of `Willard2006a`" — `../extraction/willard2006a.md` records no such confirmation. **D3's** C6 confirmation (`Willard2011` D.1(iv) ≡ `Willard2005` Eq. 20) is likewise unrecorded as done, although O2 records the substance. |

### 4.4 Content defects surfaced incidentally

| # | Finding |
| --- | --- |
| **F-C1** | §3.2's grounding-function table **omits `Willard2001`** entirely, although D23 and D80 both key their comparisons to its seven-function list, and §3.3's `Δ⁻₀` row cites the same paper. |
| **F-C2** | §3.2's `Root` rounding split lists 1998/2002c/2004/2005 as ceiling and 2001/2016 as floor, and **omits `Willard2006a`**, whose `Root(x,y) = ⌈x^{1/y}⌉` is image-verified at `../extraction/willard2006a.md` p. 6. Separately, `Willard2004`'s rounding appears in that split but is recorded by **no** drift entry — verify the anchor under R1/R2. |
| **F-C3** | §8.1 says the corpus contains "**five** parameterisations", tables five, then names `R(i,j)` among "**three** dials [that] name their transition point", where the other two (`Hybrid(H)`, `Herb−k`) are table rows and `R(i,j)` is not. Defensible — §8.1 also calls `ℜ` a projection of `R(i,j)` — but the two counts read against each other. |
| **F-C4** | §4.5 anchors `Xtab` at "`Willard2020` App."; D42 anchors its definition at `Willard2020` **p. 11**. Reconcile under R2. |
| **F-C5** | §9.4 lists "the Hájek correspondence" among "nine witness-grade records from **Willard's papers**" without marking it third-party, which D77 (`explained`) exists to prevent being read the wrong way. |
| **F-C6** | **D36** residue (the entry is otherwise dispositioned `resolved`): `ISINF` / *Infinitely Far-Reaching* is carried nowhere in `codified-sjas.md` (zero hits), although the other two things `Willard2011` drops from `Willard2006a` — the naming-convention axis and Continuously Expanding — are both carried at §5.3 and §5.5. |

---

## 5. Acceptance

**A-D — all 80 drift entries dispositioned:** satisfied for the 67 that exist,
with the 13 unused ids (D52, D58–D69) identified as deliberate gaps in §1.

**Deficiency 11 (the D29 precedent):** D29 itself is verified fully discharged
— §3.1, §5.4, §8, and the corrected `result-matrix.md` rows — with no residue.
But the walk found **twenty-two** further entries in the same class: components
whose stated obligation on the codification is undone, none of them cited at
the point where the document needed them. D29's survival through C13 was not an
isolated lapse; F1–F21 are its cohort, and F1 (D42) is worse than D29 was,
because there the document restates a reading the ledger has already withdrawn.

**Recommended sequence before A-D is signed off:** F1 first (it is a false
statement, not an omission), then F2–F4 (unqualified phrases a reader will take
as single hypotheses), then the absences F5–F12. F-L4 should be done in the
same pass as any of them, since sixteen entries currently misreport their own
state.

Nothing in this audit was committed.
