# Composition Obligations

The C14–C15 checklist. Every finding that the **codified statement itself must
act on** is appended here as it is discovered, in addition to wherever else it
is recorded (drift ledger, fidelity check, extraction record). Findings
recorded only in those places do not surface reliably at composition time —
this register exists so none is lost between discovery and composition.

Statuses: `open` (must still be acted on), `discharged:YYYY-MM-DD` (with a
pointer to where the codified statement handles it).

Kinds: `side-condition` (implicit hypothesis to state), `distinguish`
(quantities or systems that must not be conflated), `two-readings` (variants to
present with provenance rather than merge), `proof-status` (claim whose status
must be labelled), `notation` (glyph or convention to fix), `provenance`
(citation chain to preserve).

| Id | Kind | Obligation | Source | Status |
| --- | --- | --- | --- | --- |
**C15 discharge (2026-08-31).** The full draft of `../codified-sjas.md`
cites **53** of the 87 obligations at their point of discharge. The remainder
are either finer-grained than the draft's grain or belong to C16's acceptance
tests; C16 walks the register row by row and marks each `discharged` or
`carried`.

**Numbering note (C14, 2026-08-31).** Ids run O1–O80 and O90–O96; **O81–O89
were never issued**. Eighty-seven obligations, all ids unique, none lost. The
block is left unallocated rather than renumbered, because the ids are cited
from `results.md`, the drift ledger and the extraction records.

| O1 | side-condition | **Theorem 1's closing inequality needs `m ≥ 3`.** `g > h` unfolds to `m − 1 > m^{2θ}`, which for θ = 1/4 requires `m > (3+√5)/2 ≈ 2.618`. Willard (p. 20) and the affine-tree re-derivation both gloss it as immediate. Harmless — `m` is a proof's Gödel number — but the codified Theorem 1 must discharge it explicitly rather than inherit the gloss. **Scoped by C6**: this is a *2005-only* obligation — `Willard2011` Theorem 5.9 lands both a sentence and its negation on the *same* `Good{½♯(Φ)}`, so its contradiction is immediate and no analogous side condition arises | `../extraction/willard2005-fidelity-check.md` (Observation); drift D6 | discharged:2026-09-01 (C16 R5)|
| O2 | distinguish | **Three constants must stay separate** in any presentation of the compactification argument: **6** bits per language symbol (Willard2005 Appendix A, base-64 bytes); **5** in Eq. (20) bounding U-Height relative to `Log₂ p` (function symbols along a branch only, not all symbols); **1/4** the exponent θ actually proven. Conflating them is what produced drift D6. **Their relation is now explained** by `Willard1993-TR` Observation A.8: a base-32 digit is stored in a **6-bit byte**, so the godelized form costs 6/5 of the natural one — five bits of content per six-bit symbol. Hence `Willard2011` D.1(iv)'s "≥ 5J bits… Gödel number ≥ 32^J" (the 32 is the base-32 alphabet), and hence 2005's Eq. (20) constant 5 alongside its 6-bit Appendix A. Present the density as one convention viewed two ways, then θ as a separate choice. **Refined by C6**: `Willard2011` states the mature form as the **Conventional Tableaux Encoding Requirement** — a proof with `J` function symbols has Gödel number `≥ 32^J` — justified there as "at least 2J logical symbols and thus at least 5J bits", a *different derivation* of the same 5 from 1993's base-32-digits-in-6-bit-bytes route. Present the requirement (32^J), then note the two derivations | fidelity check F2; drift D3; `../extraction/willard1993-tr.md` §3.8 | cited-only:2026-09-01 (C16 R5; see C16-R5-dispositions.md)|
| O3 | two-readings | **Density bounds θ from below, not above.** State the relation as the window `1/d ≤ θ < 1/3` (non-empty iff `d > 3`), with Willard's choice θ = 1/4 at density 5 shown as a selection within it — not as an equality `θ = 1/d` and not as a ceiling `θ ≤ 1/d` | drift D6 | discharged:2026-09-01 (C16 R5)|
| O4 | proof-status | **Label the stated-only apparatus claims.** Willard2005 Remark 1 asserts consistency preservation for cut-free sequent calculus, Herbrand deduction, Tab−Q\*₁−List, and Q\*₁-restricted Hilbert with the case analysis explicitly omitted for space; Remarks 6 and 8 are likewise stated-only. The result matrix must mark these cells as asserted-not-proved, distinctly from the proved tableau and Tab−U\*₁−List cells | extraction §4; gap G8 | discharged:2026-09-01 (C16 R5)|
| O5 | provenance | **Theorem ∗ is cited and unpublished.** Solovay's generalization of Pudlák's Theorem 2.3 was never published; its only expositions are Willard's own (`Willard2001` Appendix A, 4 pp.; `Willard2005` §4). The same April-1994 telephone-communication thread is cited in `Willard2020-LFCS` ref. 41. The codified statement must present Theorem ∗ with this provenance rather than as ordinary published background | drift D4 | discharged:2026-09-01 (C16 R5)|
| O6 | notation | **Fix the glyphs the text layer destroys.** The consistency-preserving map is Fraktur **ℑ** (not `=`); the Gödel diagonalization sentence uses a mho-like script glyph (not `f`); numeral **overbars** are semantic — `Γ(n̄)` vs `Γ(n)` is numeral-substitution vs variable-substitution, i.e. the fixed point itself, and Eq. (9) reads `∃z Log^k(z) ≥ m̄` | extraction §8.1; notation registry | discharged:2026-09-01 (C16 R5)|
| O7 | distinguish | **`IS(A)` vs `IS_D(A)`.** `Willard2001`'s system (Level(0-), semantic tableaux) and `Willard2005`'s (Level(1), apparatus-parameterized) differ by one subscript, share the group architecture, and delegate Group-1 and the ∆\*₀ encodability appendices from the latter to the former. Keep them typographically distinct and state the delta once | drift D5 | carried:2026-09-01 (C16 R5; Refinement)|
| O8 | two-readings | **Record the Main Conjecture and its refutation.** `Willard1993` §6 conjectures that the cut-permitting `IS₊(A)` and `IS*₊(A)` are also self-verifying, on the grounds that humans "instinctively recognize their own self-correctness". The conjecture is false as stated: TR Proposition 7 buys cuts only by dropping addition-totality, Solovay's Theorem kills the successor-carrying case, `Willard2004` closes Tab−Σ\*₂/Π\*₂, and `Willard2005` Theorem 5 recovers only a Level-1 restricted modus ponens, proven near-maximal. The codified statement must present this arc — the origin conjecture, its refutation, and the precise surviving fragment — rather than either suppressing the conjecture or leaving it open | `../extraction/willard1993.md` §4 | discharged:2026-09-01 (C16 R5)|
| O9 | provenance | **Attribute the duplication insight to 1993.** The identification of the multiplication boundary with *variable duplication* — and its link to Gödel's diagonalization — is stated by Willard at `Willard1993` p. 328 ("two occurrences of the same variable"; "the same double appearance of a variable"). The affine-tree documents present it as a finding of their tree transposition and cite only 2005/2001/2020. Attribute it to the origin passage; the transposition's contribution is the *carrier-independence* of the identification, not the identification itself | drift D13 | carried:2026-09-01 (C16 R5; Refinement)|
| O10 | distinguish | **The two "eight-function" signatures are different languages.** `Willard1993-TR` `G₀` = Addition, Subtraction, Division, StringCount, Shift, Extract, Andreverse, Address (bit-string). `Willard2005` U-Grounding = Subtraction, Division, Root, Maximum, Logarithm, Count, Addition, Double (arithmetic). Only three symbols are common; the shared cardinality of eight is a coincidence and a trap. Never write "Willard's eight functions" without naming the source | drift D11; also D10 (the chapter lists fourteen) | carried:2026-09-01 (C16 R5; Refinement)|
| O11 | side-condition | **State why the U-Grounding language exists.** In 1993 addition-totality is an explicit **Π₂** axiom; in 2005 addition and `Double` are function symbols, so the corresponding axioms are Π\*₁ — which is precisely what lets Definition 4 require every axiom to be Π\*₁/Σ\*₁ and lets Theorem 1's proof treat the whole system as Normed. 2005 §2 motivates the language only as notational convenience; the load-bearing consequence should be stated. **Discharged in substance by C6**: `Willard2011` Definition D.1(ii) writes the axiom as `∀x∀y ∃z ≤ x+y (z = x+y)` and calls it a "very precise **Π\*₁ styled** declaration that addition is a total function" — the function symbol is what makes the bound `x+y` expressible, and the bounded existential is what keeps it Π\*₁. Quote this sentence | drift D12; `../extraction/willard2011.md` §3.6 | carried:2026-09-01 (C16 R5; Refinement)|
| O12 | notation | **1993's unstarred classes are 2005's starred ones.** `Willard1993-TR` writes Δ₀/Π₁/Σ₁ for what `Willard2005` writes Δ\*₀/Π\*₁/Σ\*₁, and the two 1993 witnesses define the classes differently from each other (TR: bounded-quantifier prefix form; chapter: majorization by enclosing universals). Reading 1993 with 2005 conventions is an error; the codified statement must fix one convention and map both | drift D9 | discharged:2026-09-01 (C16 R5)|
| O13 | distinguish | **The signature is not load-bearing; the growth class is.** `Willard1993-TR` Remark 6 states that `G₀`'s definition "was quite arbitrary" and that Propositions 1–4, 8, 9 survive the addition of *any* set of **slowly growing** functions (`f̂[i,j](x̄) ≤ i·2^j·Max(x̄)`), Proposition 7 any non-growth set. The codified statement should present the systems by growth class, listing particular signatures as instances — which is also what makes the 1993 and 2005 eight-function lists compatible (O10) | `../extraction/willard1993-tr.md` §7; drift D10/D11 | discharged:2026-09-01 (C16 R5)|
| O14 | two-readings | **Two explanations of why multiplication is fatal.** 1993: multiplication-totality makes local Π₁ reflection derivable, which Proposition 5 shows to be inconsistent. 2005: multiplication's growth overwhelms Definition 5's envelope arithmetic, so no θ exists. Present both as routes to one boundary; do not paraphrase either as the reason | drift D15 | carried:2026-09-01 (C16 R5; Refinement)|
| O15 | provenance | **"Tangible" originates in 1993.** `Willard1993-TR` §14 (printed p. 34) describes Propositions 1–4 and 7–9 as illustrating "the **limited but tangible power** of self-verifying formalisms" — the lexical origin of the *Tangibility Reflection Principle* named in `Willard1997` and `Willard2001`. Worth a sentence when the codified statement introduces that principle | `../extraction/willard1993-tr.md` §4 | carried:2026-09-01 (C16 R5; Refinement)|
| O16 | provenance | **Concatenation-strength has a 1993 statement.** `Willard1993-TR` Observation A.10 (printed p. 46): "While `IS(A)` is too weak to prove that concatenation is a total function… it certainly can represent `ConcatenateCheck_m` as a Δ₀ formula." The affine-tree design argues the concatenation/multiplication-strength point from the external literature (Quine, Grzegorczyk, Visser, Murwanashyaka); Willard states the system-side fact himself, and handles it by the same demote-to-relation move. Cite the origin alongside the literature | `../extraction/willard1993-tr.md` §3.8 | carried:2026-09-01 (C16 R5; Refinement)|
| O17 | distinguish | **Fixed-constant multiplication was always total.** `Scalar_k` is a function in `IS(A)` ("Unlike normal multiplication, scalar multiplication is a function under `IS(A)`", printed p. 39), and `Willard2005`'s `Double` is exactly `Scalar₂`. "Multiplication is not total" therefore means *variable* multiplication; the codified statement must say so, or the whole boundary reads as stronger than it is | `../extraction/willard1993-tr.md` §3.8 | carried:2026-09-01 (C16 R5; Refinement)|
| O18 | two-readings | **Axiom versus theorem under cut-free deduction** is a named mechanism, used at 1993 (INVALID.1 vs INVALID.2) and 2005 (`IS_D` vs `NS^{k,m}_D`). Introduce it once as a device and refer back, rather than re-deriving it per paper | drift D16 | discharged:2026-09-01 (C16 R5)|
| O19 | proof-status | **Flag the Meta-Logic convention.** `Willard1993-TR` Remark 2 establishes many results by applying cut at the meta level and omitting the cut-free construction. Any codified restatement of a 1993 proof must say whether the original constructs the proof or only establishes its existence — and must not let a cut-free *object* system be confused with a cut-free *metatheory* | drift D17 | carried:2026-09-01 (C16 R5; Refinement)|
| O20 | provenance | **Trace-carrying proof predicates are Willard's, from 1993.** Lemma 5.4's `Prf2_B(x,y₁,y₂)` pairs a proof with a Turing-machine trace verifying its well-formedness, and `IS2` built on it makes the whole Appendix A arithmetization dispensable; the chapter offers the same under `Prf*_A`. The NC-SJAS plan and affine-tree design treat explicit-trace checking (`Step`/`TraceOK`, `EvalTraceOK`) as a native-computational move; cite the 1993 origin | `../extraction/willard1993-tr.md` §3.7b | carried:2026-09-01 (C16 R5; Refinement)|
| O21 | provenance | **Willard states the delicacy thesis himself.** `Willard1993-TR` printed p. 12: "a **modest change in a formalism's definition will cause Propositions 1 thru 7 and Solovay's Theorem to produce starkly different results**… it appears premature to develop hasty conclusions". This is the premise of `prose/sjas_synthesis.txt`, asserted by Willard about his own results in 1993. Quote it where the codified statement explains why variants are tracked rather than merged — the caution is the corpus's own lesson, not an external methodological choice | `../extraction/willard1993-tr.md` §3.7c | carried:2026-09-01 (C16 R5; Refinement)|
| O22 | distinguish | **Fixed-parameter totality versus uniform totality is the corpus's recurring device.** `IS(A)` proves the second Hilbert–Bernays condition (provable modus ponens) only "in the *degenerate case* where `x` and `y` are *fixed constants*" (`Willard1993-TR` p. 12); `SUBST_i(k̄,y)` holds for each fixed `k̄` but not uniformly (p. 37); Lemma 5.3's `T(p,k)` is total for fixed `k` or fixed `p`, never jointly (p. 17); `Scalar_k` is a function for each fixed `k` while multiplication is not (p. 39). Present this as one mechanism with several instances, rather than re-deriving it per restriction; it also names exactly which derivability condition fails | `../extraction/willard1993-tr.md` §3.7c | carried:2026-09-01 (C16 R5; Refinement)|
| O23 | provenance | **Willard 2011 answers the finite-axiomatization question asked in this repository.** `prose/to-dw-20201206z.txt` question 2 asks whether Group-2's infinitary schema can be replaced by a finite set sufficient to construct all Π\*₁ theorems. `Willard2011` answers it in two stages: a single **Global Simulation Sentence** replaces the schema but yields only the Test-relativized form `∀x Test^ξ_j(⌜Ψ⌝,x)` (Definition 6.8, Remark 6.9); **three** added sentences yield the theorems *purely*, but only for a quantifier-count bound `c` fixed in advance (Theorems G.2, G.3). The codified statement should present this as the settled answer, with its two-stage structure and its cost | `../extraction/willard2011-subsumption-audit.md` §4 | cited-only:2026-09-01 (C16 R5; see C16-R5-dispositions.md)|
| O24 | proof-status | **Carry Willard's own two barriers.** `Willard2011` Remark 6.16 states them: (a) Theorem 2.1 and its tableaux analogues bound what can be evaded; (b) **`SelfCons` yields "essentially a 1-line proof" of consistency — "an instinctive faith in its own consistency (rather than … a full-length proof-justification of this fact)"**. Any presentation that omits (b) overstates what SJAS delivers | `../extraction/willard2011.md` §4 | discharged:2026-09-01 (C16 R5)|
| O25 | distinguish | **Three names, three predicates.** 1993 *self-verifying* (property of a system, no deduction method), 2005 *Self-Justifying* (property of α, ∃d) and *Introspectively Unified* (property of the pair), 2011 *Self Justifying* (property of the pair). Fix one vocabulary and tabulate the others; never let the shared word carry the argument | drift D18, D7 | carried:2026-09-01 (C16 R5; Refinement)|
| O26 | distinguish | **Willard redefines nothing — the theory is weakened, not the vocabulary.** "Total function" keeps its standard meaning throughout (`∀x∀y∃z Mult(x,y,z)`); what varies across the corpus is *which totality axioms a system takes* and *which function symbols its language carries*. `Willard1993-TR` p. 3 is explicit that `IS(A)` "will recognize non-zero Division as a total function (and hence can view Multiplication as a well-defined **partial** function)", and `Willard2011`'s Type-M / Almost-M / A / S / NS taxonomy is purely a question of which of axioms (1)–(3) are present. The codified statement must never say or imply that SJAS uses a weaker *notion* of totality (or of provability) — that reading turns an honest weakening of the theory into a definitional trick, and is precisely the Feferman-style "numerically correct but intensionally incorrect" route Willard rules out in `Willard1993` p. 325. The contrast to draw: Rosser-style self-verification alters the *provability predicate* on a fixed theory; Willard alters the *theory* and leaves every notion standard | `../extraction/willard1993-tr.md` §3.2; `../extraction/willard2011.md` §3.1 | discharged:2026-09-01 (C16 R5)|
| O27 | side-condition | **State the negation delicacy explicitly; do not inherit it as a gloss.** The engine's closing step needs "Υ and ¬Υ cannot both be `Good(N)`", but `Scope_E(¬Υ,N)` is **not** the syntactic negation of `Scope_E(Υ,N)` — the rebounding is applied to each sentence's own prenex form. For a Π^ξ₁ Υ and its Σ^ξ₁ negation the two results *do* coincide with the complementary bounded statements `∀v̄ < E(N) φ(v̄)` and `∃v̄ < E(N) ¬φ(v̄)`, so the impossibility is immediate — but that coincidence is a fact to be stated, not assumed. It is the 2011 counterpart of the delicacy `Willard2005` manages in **footnote 3** (for a proper Σ\*₁ Υ the string `¬Υ` is not itself syntactically Π\*₁, since a negation sits left of the unbounded quantifiers) and in **Remark 5** (the Prenex\* conventions I and II, which Willard says are dispensable but simplifying). The codified engine presentation must (a) fix a convention for negation and prenex form, (b) show that under it the two bounded statements are exact contradictories over the common window `< E(N)`, and (c) note that the Π/Σ polarity opposition (2011 footnote 6) is what makes "Good at the same level" contradictory rather than redundant | `../discussion/2026-08-21-engine-machinery-and-rosser.md`; `../extraction/willard2005.md` §4 (Lemma 2, Remark 5); `../extraction/willard2011.md` §3.3 | discharged:2026-09-01 (C16 R5)|

| O28 | present | **The three tangibility predicates are indexed, not interchangeable.** `Willard2001` §1 results (i)/(ii) tie each to an apparatus and a totality budget: Hilbert/Gentzen-with-cuts + `TangPred` + neither Addition nor Multiplication (`ISREF(A)`); Hilbert/Gentzen-with-cuts + `TangDiv₂` + **Bitwise-Or** total (`ISTR(A)`); cut-free (Herbrand/Tableaux) + `TangRoot` + **Addition** total (`IS^λ(A)`). Present this as a table — it is the corpus's most compressed statement of its own design space, and it has no counterpart in 2005 or 2011 | `../extraction/willard2001.md` §3.4 | carried:2026-09-01 (C16 R5; Refinement)|
| O29 | distinguish | **Tangibility is a definable cut minus closure under successor.** `Willard2001` p. 5 defines a Definable Cut by (a) `Υ(k̄)` for each fixed `k`, (b) downward closure, (c) closure under successor, and names (a)+(b) alone the weaker **Tangibility Criteria**. That single dropped clause is the whole difference between Willard's program and the Nelson / Pudlák / Wilkie–Paris tradition, and it explains criterion III: `ISREF(A)` cannot see `TangPred` as a cut (no Successor totality) while `IS^λ(A)` *can* see `TangRoot` as one. State it once, early; it makes the relation to the prior literature exact rather than atmospheric | `../extraction/willard2001.md` §3.5 | carried:2026-09-01 (C16 R5; Refinement)|
| O30 | present | **Show why coding density is load-bearing, using 2001's Case 11.** The Tertiary deduction `t₁ = u − t₂` at most **doubles** a parameter's bound (one unit of the `2^{β/6}` budget), while encoding the sentence that performs it costs `β_i − β_{i−1} ≥ 6` bits — so the budget covers addition and nothing faster, and `2·(k+√y)·2^{⌊β_{i−1}/6⌋−2} < (k+√y)·2^{⌊β_i/6⌋−2}` closes exactly. `Willard2001` p. 28 is the clearest statement of this anywhere in the corpus, and it is the origin of drift D6 (density bounds θ from *below*). Pair it with Remark 4.5's `u_{i+1} = u_i²` giving `2^{2^n}`, which localises the multiplication failure to the loss of Lemma 4.2 alone | `../extraction/willard2001.md` §3.6 | carried:2026-09-01 (C16 R5; Refinement)|
| O31 | distinguish | **Consistency-preservation and self-knowledge are two properties; multiplication separates them.** `Willard2001` §6: `ISM(A)` recognises **Multiplication as total** and *is* consistency-preserving, but its wider Group-3 gap `y·Log₂(x+2) < Log₂ z` is too weak to yield "+++" ("I am unable to produce a Semantic Tableaux proof of 0=1"); footnote 14 shows exactly what survives. Willard: the two systems "have quite different philosophical and epistemological implications, although the strictly formalistic mathematical proofs of their consistency are virtually identical." The codified statement must not present multiplication-totality as simply fatal — it is fatal to *reflection* (Thm 7.3), not to *consistency-preservation* | `../extraction/willard2001.md` §3.7 | discharged:2026-09-04 (C16 §7.2; `../refinement/refined-sjas.md` §9)|
| O32 | present | **State the margin.** `Willard2001` Remark 3.6: Theorem 3.5 gives `ISREF(A) ⊢ ∀y {HilbPrf(⌜Ψ⌝,y) ⊃ Ψ^{y−1}}`, and Theorem 7.2 shows **no consistent axiom system of any kind** can prove the same with `y−1` strengthened to `y`. The attainable and the impossible are one apart. This single pair does more to convey what SJAS is than any amount of machinery, and it belongs in the motivation section, not buried in the boundary chapter | `../extraction/willard2001.md` §3.7 | carried:2026-09-01 (C16 R5; Refinement)|
| O33 | resolve | **State the encoding once, with the bits/payload distinction explicit.** A byte is six bits, five of them base-32 payload plus one tag; formula encodings are base 64. 2001's `β/6`, 2005's `(1/5)Log₂p`, 2011's `32^J` and 1993's `i^{6/5}` are four measurements of that one scheme, and presenting them without the distinction manufactures a contradiction that is not there (drift D25) | `../extraction/willard2001.md` §3.8; drift D25 | carried:2026-09-01 (C16 R5; Refinement)|
| O34 | provenance | **The grounding-function set is chosen partly to make the arithmetization cheap.** `Willard2001` Theorem C.2 (p. 59): because `Count` and `Logarithm` are Δ⁻₀ **primitives**, Willard "simply avoid[s] the hardest part of the proofs in [13, 18, 46]" — Bennett's exponentiation encoding and the `NUON(x) = COUNT(x,∞)` construction that IΣ₀ needs. Say this where the language profile is introduced; otherwise the seven functions read as an arbitrary list rather than a design with a stated payoff | `../extraction/willard2001.md` §3.8 | carried:2026-09-01 (C16 R5; Refinement)|
| O35 | present | **Carry Willard's no-ideal-system verdict.** `Willard2001` §1 lists seven criteria (I–VII) on which self-verifying systems can be compared, credits the definable-cut literature with I–IV and his own systems with V–VII, and concludes: "there are inherent tradeoffs that prevent a self-justifying axiom system from possessing all of features (I) through (VII), simultaneously! Thus, **it is futile to seek an idealized form of self-verifying system**". This is a direct constraint on the Refinement stage's stated ambition, asserted by Willard about his own program — the codified statement must present the trade-off space rather than nominate a best system, and Refinement must engage this claim rather than inherit it silently. Pair with O21 (the delicacy thesis) and O24 (the two barriers) | `../extraction/willard2001.md` §3.9 | discharged:2026-09-01 (C16 R5)|
| O36 | provenance | **The finite-set reading of SJAS starts in 2001, not 2008.** `Willard2001` Remark 3.8: `ISTR(A)`'s Bitwise-Or formalises finite-set **union**, and Group-2 supplies bitwise set-subtraction and intersection, so `ISTR(A)` recognises the three basic finite-set operations as total alongside a TangDiv₂ formalisation of its Hilbert consistency and PA's Π⁻₁ theorems — "Could this be a partial … explanation for how Human Beings seem to have an instinctive sense of their Self-Consistency…?" This is the earliest anchor for the nachlass ZCF/ZF program (C12) and should be cited there rather than treated as a late idea. Note the proofs are not in 2001 — they are in the unwitnessed `Willard1997` (gaps G2, G17) | `../extraction/willard2001.md` §3.10 | discharged:2026-09-01 (C16 R5)|
| O37 | distinguish | **The negative results are non-constructive; the positive ones are not.** Every affirmative theorem in the corpus is a minimal-witness construction carrying explicit size bounds (`Willard2001` Lemmas 4.2/4.7/4.8, `Willard2005`'s θ-compactification, `Willard2011`'s ♯). The Second-Incompleteness-style refutations run the other way: `Willard2001` Theorem 7.2 establishes that `α ⊢ Θ` and `α ⊢ ¬Θ` both hold, but reaches (69) by stepping **outside** α — a proof of `Θ` exists, therefore `SemPrf_α(⌜Θ⌝,m̄)` is a true Δ⁻₀ sentence, therefore α proves it — with no bound on either derivation. Theorem 7.3's inconsistency of `XIS^λ(PAX)` inherits this. The codified statement should say which side of each result is constructive; readers otherwise assume the boundary theorems carry the same explicit content as the engine theorems, and they do not | `../extraction/willard2001.md` §3.7 | discharged:2026-09-01 (C16 R5)|
| O38 | present | **The apparatus axis is extensionally degenerate and intensionally decisive — say so.** `Willard2020` Example 3.1: "while proofs have **different lengths** under different apparatuses, all the common apparatuses produce the **same set of final theorems** from an initial common axiom basis" (footnote 2: because all satisfy Gödel Completeness), restated at §7 for Z-enrichment — "yet despite this invariance, the proof-lengths change, quite sharply". Every apparatus distinction the codification tracks (Hilbert vs tableau vs Herbrand vs resolution, Tab vs Xtab vs Tab-1) is invisible at the level of theoremhood and decisive at the level of proof length. State this once, up front, or the whole apparatus axis reads as pedantry | `../extraction/willard2020.md` §3.1 | discharged:2026-09-01 (C16 R5)|
| O39 | present | **Lead the growth restriction with 2020's object-level formulation, not the bit-counting one.** `Willard2020` §5: with `x₀=y₀=2`, `xᵢ = xᵢ₋₁+xᵢ₋₁` and `yᵢ = yᵢ₋₁*yᵢ₋₁`, we get `xₙ = 2^{n+1}` and `yₙ = 2^{2ⁿ}`, so `Log(xₙ) = n+1` while `Log(yₙ) = 2ⁿ`. The multiplicative sequence's encoding is *long enough to hold the derivation that defines it*; the additive one's is not. That is the whole mechanism, stated about numbers rather than about proof-encodings, and it is far more intelligible than `Willard2001`'s `β_i − β_{i−1} ≥ 6` or `Willard2005`'s `(1/5)Log₂p`. Present it first and derive the bit-counting forms (O30, O33) from it | `../extraction/willard2020.md` §3.8 | discharged:2026-09-01 (C16 R5)|
| O40 | distinguish | **Proof compression is the active ingredient of the Second Incompleteness Effect.** `Willard2020` §6 isolates it: Hilbert-Frege deduction has the **"Linear-Sum Effect"** (modus ponens bounds a proof of `ψ` by the sum of the lengths for `φ` and `φ → ψ`); Tab lacks it; **Xtab recovers it** by admitting `φ ∨ ¬φ` as a logical axiom, via an explicit four-step tree. Statement ⊙ then generalises Theorem ++ from Hilbert-Frege to Xtab "because both satisfy the Linear-Sum Effect". So what activates the theorem is neither expressive power nor induction nor theoremhood, but the availability of a linear-sum proof-composition step. This is the single most transferable idea in the corpus and the natural hinge for the Refinement stage | `../extraction/willard2020.md` §3.9 | discharged:2026-09-01 (C16 R5)|
| O41 | provenance | **Willard's own Level-0 / Level-1 comparison is the justification for treating levels as an axis.** `Willard2020` p. 22: the 1993/2001 `IS(A)` "was actually **simpler**, but it was substantially **weaker** because it only recognized the non-existence of a proof of 0 = 1 from itself. In contrast, `IS_{Tab-1}(β)`'s Group-3 axiom can corroborate that no two simultaneous proofs exist for a Rank-1\* sentence and its negation. This is an important distinction, because the **First** Incompleteness Theorem indicates no decision procedure exists for separating all true from false Rank-1\* sentences." Quote this where the levels are introduced — it supplies the *reason* the distinction is substantive, and it is Willard grading his own two decades | `../extraction/willard2020.md` §3.12 | discharged:2026-09-01 (C16 R5)|
| O42 | side-condition | **Fix one tableau definition, and say whether the root is prenex\*-normalised.** `Willard2001` §4 requires a tableau proof's root to store `¬Φ` **rewritten in prenex\* normal form**, and its footnote 8 leans on that normalisation to force the closing pair `Ψ`/`¬Ψ` to be `Δ⁻₀` — which is what makes Lemma 4.6 work. `Willard2020`'s Appendix defines tableau with **no** such requirement. Willard notes in 2001 that the requirement is dispensable ("All our theorems will also hold if we drop the prenex\* requirement"), but the codified engine presentation must state which convention it adopts and re-establish the `Δ⁻₀` closing-pair fact under it, rather than importing Lemma 4.6 verbatim into an unnormalised setting. Pairs with O27 | `../extraction/willard2020.md` §7 item 7; `../extraction/willard2001.md` §3.6 | discharged:2026-09-01 (C16 R5)|
| O43 | present | **One branching-axiom construction, two instantiations.** `Willard2002a` Lemma 6.3 and `Willard2020` §6 give the same five-/four-step tableau construction — root `¬Ψ`, a branching axiom, a split, the two subproofs grafted below the two children — differing only in whether the branching axiom is an implication `Θ_Υ ⇒ Υ` or a LEM instance `φ ∨ ¬φ`. Present it once, then instantiate. Doing so also explains `Willard2002a`'s `V₄` CLARIFICATION (a redundant axiom super-exponentially shortens cut-free proofs) and `Willard2001` Lemma 7.1's comment as the same observation | drift D30 | discharged:2026-09-01 (C16 R5)|
| O44 | present | **The doubling/squaring pair is the corpus's single unifying device — say so once and reuse it.** `x₀=y₀=2`, `xᵢ = xᵢ₋₁+xᵢ₋₁`, `yᵢ = yᵢ₋₁·yᵢ₋₁` appears as: `Willard2001` Remark 4.5 footnote 7 (`2^{2ⁿ}` breaks Lemma 4.2, so the positive engine collapses under multiplication); `Willard2002a` Lemma 4.7 (the same squaring reaches `z` in `O(LogLog z)` steps, which is what makes the *negative* proof's subtree short); `Willard2006a` Eqs. (2)–(3) (the additive and multiplicative **naming conventions**, an explicit design axis); `Willard2020` §5 (`Log(xₙ)=n+1` vs `Log(yₙ)=2ⁿ`, so only `yₙ` can encode its own derivation). One device, four papers, both sides of the boundary — and the clearest way to show that the positive and negative results are two readings of one fact | `../extraction/willard2006a.md` §3 | discharged:2026-09-01 (C16 R5)|
| O45 | present | **The consistency-level index is a real axis, and `Willard2004` says why.** Level(0-) is "no proof of `0=1`"; Level(n) is "no `Π*ₙ` sentence and its negation are both provable"; Level(n+) uses `Q*ₙ` sentences (Boolean combinations). Willard: "All definitions … are equivalent to each other under strong enough models of Arithmetic. However, **many weak axiom systems do not have a mathematical strength to formally prove and recognize this equivalence.**" That sentence is the justification for indexing systems by level rather than treating "consistency" as one notion, and it pairs with O41's Level-0/Level-1 comparison | `../extraction/willard2004.md` §3 | discharged:2026-09-01 (C16 R5)|
| O46 | distinguish | **Do not merge `Willard2004`'s `ℜ` dial with `Willard2020`'s `Z` dial.** 2004 parameterises which *intermediate theorems* a TabList proof may use; 2020 parameterises which *LEM instances* are logical axioms. Both yield a `Π*₁`-level positive and a `Π*₂` negative, which invites conflation — but on 2004's dial the `Π*₁ ∪ Σ*₁` case is affirmative though only **`sketch`** (Theorem 3, at Level(1); gap G35 accepts that status as permanent) while on 2020's it is **conjectured**. Stating both dials separately is what makes the open conjecture legible: it is the claim that the 2004 result transfers | drift D34; gap G22 | discharged:2026-09-01 (C16 R5)|
| O47 | provenance | **`ISCE(A)` is `ISREF(A)` with the naming convention upgraded.** `Willard2006a` says its Theorem 3's degenerate, incremental-naming version "was established by the Theorem 3.4 of [46]" — i.e. `Willard2001`'s `ISREF(A)` consistency-preservation. The upgrade from incremental to **additive** naming buys the **Continuous Expansion** property, which the incremental convention fails because "it grows too slowly". Present the two systems as one construction at two naming conventions, with continuous expansion as the stated payoff, rather than as two Hilbert-apparatus systems | `../extraction/willard2006a.md` §3 | discharged:2026-09-01 (C16 R5)|
| O48 | present | **Restore the missing middle of statement ⊙'s justification from the LFCS chapter.** `Willard2020` says ⊙ "rests upon the very detailed 'Definable Cut' machinery from pages 172-174 of the Hájek–Pudlák textbook" and gives no more. `Willard2020-LFCS` Appendix B supplies the chain: LEM-as-logical-axiom → the Linear-Sum Effect → short proofs of the **Locally-J-Closed** conditions (11)/(12) → the definable-cut diagonalization; with footnote 7 noting only **Locally-1-Closure** is actually needed. The codified statement should carry the full chain and cite the LFCS chapter for the middle links, since the arXiv version dropped them | `../extraction/willard2020-lfcs.md` §3 | carried:2026-09-01 (C16 R5; Refinement)|
| O49 | present | **State the boundary once, then give its five coordinatisations.** `Willard2004`'s `ℜ` and **Level(n)** dials, `Willard2006a`'s `Hybrid(H)`, `Willard2009`'s `Herb−k` and `Willard2020`'s `Z` are five parameterisations of one phenomenon, and Willard never relates any two. For each, say whether the transition is proved, sketched or conjectured: `Hybrid(H)`, `Herb−k` and `Willard2002c`'s `R(i,j)` each state a threshold exactly (D46); **Level(n)** is the only dial proved on both halves; `ℜ` is **sketch on both halves**; `Z`'s both sides are stated-only with `Π*₁` open. **Corrected 2026-09-04:** this obligation was discharged carrying three claims that had already been retracted elsewhere — "three coordinatisations" (D38's table lists five), "`Hybrid(H)` alone has its threshold stated exactly" (retracted by D38's 2026-08-27 amendment), and "`ℜ`'s positive side is proved" (G35 accepts Theorems 3 **and** 2 as permanently `sketch`; status corrected 2026-08-27). `codified-sjas.md` §8.1 states all three correctly, so the discharge stands on the document; the defect was in the obligation text | drift D38 | discharged:2026-09-01 (C16 R5)|
| O50 | present | **`Willard2006a` Remark 3 is the cleanest statement of the growth restriction in the corpus — lead with it.** "every proof of the existence of an integer `N` using the **additive** convention will have a **larger** Gödel number than `N`. In contrast, the **multiplicative** convention allows for the Gödel number of such proofs to have a **sharply smaller** magnitude than `N`… this difference in magnitude is the reason the Second Incompleteness Theorem will generalize under the multiplicative naming paradigm." That is O44's device stated as a comparison between a proof's Gödel number and the integer it proves to exist — more direct than `Willard2020` §5's encoding-length version and far more direct than the bit-counting forms (O30, O33) | `../extraction/willard2006a.md` §3 | discharged:2026-09-01 (C16 R5)|
| O51 | distinguish | **Density is load-bearing for tableaux and merely clarifying for Hilbert.** `Willard2006a` twice disclaims the necessity of compressed encodings for its own results ("All our theorems have analogs under such uncompressed encoding methods, but they are substantially more meaningful…"), where the tableaux engine genuinely fails without it. Do not present the coding-density requirement as universal | drift D41 | carried:2026-09-01 (C16 R5; Refinement)|
| O52 | present | **Introduce both metatheoretic toolkits once, with Willard's own reason.** Definable cuts and thinnings for the Hilbert-side results; generalized Gödel sentences `⧫^λ_D(α)` for the cut-free ones, "because Definable Cuts are not always ideally suited… when D represents a cut-free deduction method" (`Willard2006a` p. 12). `Willard2004` is the explicit hybrid | drift D40 | cited-only:2026-09-01 (C16 R5; see C16-R5-dispositions.md)|
| O53 | present | **Carry both frontier problems, not just one.** Alongside `Willard2020`'s `Π*₁` conjecture (G22), `Willard2006a` records **Pudlák's Extender-Function question** (G27): can an injective non-zero `F` build a self-justifying system of Infinite Far Reach? Willard conjectures yes and says that would be "close to the maximal type of such a formalism that is feasible under at least Hilbert style deduction", with monotonicity provably fatal (Theorem 7). The frontier chapter needs one item from each side of the corpus — tableaux and Hilbert | `../extraction/willard2006a.md` §5 | cited-only:2026-09-01 (C16 R5; see C16-R5-dispositions.md)|
| O54 | provenance | **`Willard2004`'s appendix already contains the envelope machinery.** Under the name **G-good**: a `Σ*₁` sentence is G-good iff it has a `Tab₁List` proof `H ≤ G` with witnesses bounded by `2^{ℑ(H)}`; a `Π*₁` sentence iff its universal bounds are `G·2^{−ℑ(H)}`; and "it is impossible for both `X` and `Y` to have G-good proofs". That is `Willard2005`'s `Normed(a,b)` and `Willard2011`'s `Good(N)` — rising `Σ` bound, falling `Π` bound — together with obligation **O27**'s impossibility step, in 2004. Present the envelope as one construct with a dated lineage rather than as 2005's innovation | `../extraction/willard2004.md` §7 | discharged:2026-09-01 (C16 R5)|
| O55 | provenance | **Present `Xtab` and the LEM-equals-cuts identification with their 1993/94 provenance.** TR 93-10 printed p. 33 defines "a **tableaux proof with cuts**" as tableaux plus `Φ ∨ ¬Φ` at any node — verbatim `Willard2020`'s `Xtab` — — a definition, and the sole basis for this obligation. (Printed p. 36 adds a hedged parenthetical that **ZF** "can be viewed as performing deduction *with Gentzen-style cuts*, if ZF is defined to include the axiom `Φ ∨ ¬Φ` for each sentence `Φ`"; that corroborates the association but is not, and must not be cited as, a stated identification of LEM-as-axioms with cuts.) `Willard2020`'s contribution is therefore the *negative theorem* about an apparatus the corpus had defined at the outset, not the apparatus itself. Presenting it otherwise misdates the corpus's central idea by twenty-seven years | drift D42 | discharged:2026-09-01 (C16 R5)|
| O56 | present | **Carry the TR's justification for the `Δ₀` end of the Z dial.** `Willard2020` §7 asserts the `Δ*₀` evasions survive and declines to justify it; TR 93-10 printed p. 35's footnote both performs the construction (add `Φ ∨ ¬Φ` as an axiom for each `Δ₀` `Φ`) and gives the reason it is harmless — `IS(A)` already simulates a `Δ₀` cut rule "with only a polynomial increase in proof length", because Group-2 supplies `Prf_A(⌜Φ ∨ ¬Φ⌝) ⊃ Φ ∨ ¬Φ`. State the reason, since it is what makes the open `Π*₁` question precise: whether Group-2 can still deliver those instances cheaply one level up | drift D43; gap G22 | carried:2026-09-01 (C16 R5; Refinement)|
| O57 | present | **Derive the corpus's two branches from Solovay's theorem, as the TR does.** Solovay forbids (a) cuts and (b) Addition-totality together; TR Propositions 1 and 7 relax one each, and every later system descends from one branch or the other — the tableaux line from Proposition 1, the Hilbert line (`ISREF`, `ISCE`, `ISINF`) from Proposition 7. Introducing the two lines as parallel programmes, rather than as the two ways out of one theorem, loses the corpus's organising idea | drift D44 | discharged:2026-09-01 (C16 R5)|
| O58 | provenance | **"Tangible" is a 1993/94 word, and 2011's silence is an abandonment.** TR printed p. 30 already describes man as able "to recognize and perceive the truth of **only very tangible `Δ₀` sentences**", years before `Willard2001` formalises `TangPred`/`TangDiv_k`/`TangRoot_k`. Since `Willard2011` contains **zero** occurrences of "tangib" (C6 finding), the codified statement should present tangibility as a founding notion that the unification paper drops, not as a mid-period device that faded | `../extraction/willard1993-tr.md`; C6 subsumption audit | carried:2026-09-01 (C16 R5; Refinement)|
| O59 | present | **State the rule that governs which consistency notion each result uses.** `Willard2002c` p. 2: "it is desirable to use the **weakest** possible definition among the alternatives `D₁, D₂, D₃ …` when one is seeking to **generalize** the Second Incompleteness Theorem. On the other hand, the opposite is true when one seeks to develop **boundary-case exceptions** … They become more wide-reaching when they use the **strongest feasible** `D_i` available." This explains, in one sentence, why `Willard2002a` and `Willard2004` work at Level(0-) while `Willard2002c`, `Willard2005` and `Willard2020` push to Level(1) — the notion is forced by which side of the boundary a result is on. Without it the Level index looks like an arbitrary parameter that different papers set differently | `../extraction/willard2002c.md` §3 | cited-only:2026-09-01 (C16 R5; see C16-R5-dispositions.md)|
| O60 | present | **Give the boundary its two-dimensional coordinate, not just the projection.** `Willard2002c`'s `R(i,j)` admits intermediate theorems of `Π*ᵢ` **or** `Σ*ⱼ` format, and reports the collapse at **`R(1,1) → R(2,1)`** — raising the `Π` index alone. Every successor collapses the pair to one class (`Tab₁List`, `Tab-U*₁-List`, `Tab-1`, and `Willard2004`'s `Tab-Π*₂`/`Tab-Σ*₂`), which cannot express that. Present `R(i,j)` as the general form and the later dials as its projections; it is also the sharpest located threshold in the corpus | drift D46 | discharged:2026-09-01 (C16 R5)|
| O61 | distinguish | **`Willard2009` evades by re-axiomatising, not by weakening.** Every other affirmative result in the corpus drops an axiom — multiplication-totality, successor-totality — or restricts the apparatus. `Ax-3` proves **exactly the same theorems** as the conventional `Ax-1` and `Ax-2` axiomatizations of `IΣ₀` (Theorem 1), yet `Ax-3*` is consistent and recognises its own Herbrand consistency (Theorem 4). So the codified statement needs an **axiomatization axis** alongside the growth, apparatus, naming-convention and consistency-level axes: two presentations of one theory can differ in their Second-Incompleteness behaviour. The mechanism is Split Representations — base-`(a₀+1)` digit tuples that let every quantifier bound be a `Max` | `../extraction/willard2009.md` §2 | discharged:2026-09-01 (C16 R5)|
| O62 | provenance | **The Paris–Wilkie 1981 question is answered in two directions by two Willard papers.** Its two halves ask whether `IΣ₀` satisfies G2 for semantic tableaux and for Herbrand deduction. `Willard2002a` Theorem 6.4 answers the **tableaux** half **affirmatively** (every consistent extension with the Conventional Deciphering Property fails to prove its tableaux consistency); `Willard2009` Theorem 4 shows the **Herbrand** half **fails** for a suitable axiomatization. Present them as one question with a split answer, not as two unrelated results | `../extraction/willard2009.md` §2 | discharged:2026-09-01 (C16 R5)|
| O63 | distinguish | **Threshold versus Anti-Threshold, and why `⊇` means axioms.** `Willard2009` Definition 1 gives the corpus its cleanest vocabulary for boundary results: `(α,D)` is a **Threshold** iff *every* consistent `α* ⊇ α` fails to prove its `D`-consistency, an **Anti-Threshold** otherwise — with `⊇` explicitly **axiom containment**, "stronger than the more modest construct that α proves all β's theorems". That axiom-versus-theorem distinction is the same one that defines `Willard2011`'s Type-Almost-M and drives `Willard2020`'s LEM result (drift D29). Adopt the Threshold/Anti-Threshold vocabulary and state the `⊇` convention once | `../extraction/willard2009.md` §3 | discharged:2026-09-01 (C16 R5)|
| O64 | present | **Carry `Willard2009`'s own counterweight.** Theorem 4 shows `Ax-3` evades the **Herbrandized** effect; **Theorem 5** (Appendix B) shows the *same* `Ax-3` **obeys** the **semantic tableaux** one, and Willard flags the limitation in §2 before proving it. Quoting Theorem 4 without Theorem 5 would misrepresent the result as a general evasion. The pair also realises concretely Kolodziejczyk's [18,19] finding that Herbrand and tableaux consistency are provably inequivalent in some bounded arithmetics | `../extraction/willard2009.md` §4 | discharged:2026-09-01 (C16 R5)|
| O65 | present | **Quote Willard's own summary judgement on the programme's limits.** `Willard2016` §9: "All our published articles about self-justifying arithmetics have emphasized that evasions of the Second Incompleteness Effect rested on using arithmetics that were **weaker than traditional arithmetics** in, at least, some respects. (The Second Incompleteness Theorem's significance in refuting the original objectives of Hilbert's Consistency Program is thus, simply, **undeniable**.)" And Remark 8.3's 25-year retrospective concedes that a proof resting on an "I am consistent" axiom is "a **quite skinny form of proof**, that one is almost first tempted to ignore". With O24 (the 1-line-proof caveat) and O35 (no ideal system exists), these are the three places Willard states the programme's limits in his own voice; the codified statement should carry all three rather than leaving the reader to infer them | `../extraction/willard2016.md` §5 | discharged:2026-09-01 (C16 R5)|
| O66 | present | **Flag `Willard2016`'s conditionality wherever its result is used.** Theorem 6.7 — IQFS's consistency preservation — holds **only if** Conjecture 6.6 does, and Remark 6.8 says so: this is the one consistency-preservation proof in the corpus routed through an unproved intermediate. `Willard2020` §8 nevertheless draws a strong consequence from it ("if a conjecture stated in [55] is correct, as we are almost certain it is, then such a self-justifying machine will be plausible for constructing the entire set of natural numbers…"). Any presentation of the θ line must carry the conditional, and the frontier chapter should state where the appendix's argument falls short: it bounds the canonical route to large integers, not every route | `../extraction/willard2016.md` §4; gap G21 | discharged:2026-09-01 (C16 R5)|
| O67 | present | **Present the boundary dial as apparatus-independent, and say on what evidence.** `Willard2009` §6's Items I and II give the same `k = 1` positive / `k = 2` negative split as `Willard2005`'s K-deduction and `Willard2002c`'s `R(i,j)`, but under **Herbrand** deduction rather than tableaux. That is the corpus's only evidence that the threshold is a property of the *cut-restriction*, not of the deduction method carrying it. It must be carried with its weakness stated: **both halves are `stated-only`**, Willard declining to prove them because "each has a rather long proof". Drift D33, D38 | `../extraction/willard2009.md` §4 | discharged:2026-09-01 (C16 R5)|
| O68 | present | **Do not present `Δ^R₀`'s Max-only restriction as load-bearing.** `Willard2009` Remark 2 states that Theorem 4 also holds if the bounded quantifiers' terms may include **Addition** as well as `Max`, and that the omission was "for the sake of simplifying the presentation". Every other restriction in the corpus's positive results is at or near a threshold; this one is explicitly not, and the codified statement should say so rather than leaving a reader to assume the class was chosen minimally | `../extraction/willard2009.md` §4 | carried:2026-09-01 (C16 R5; Refinement)|
| O69 | present | **Carry the `Diag(1)`/`Diag(2)` contrast from the conference witness.** `Willard2006-WoLLIC` p. 10 states that the diagonal sentences for the two *conventional* axiomatizations of `ISigma_0` are **provably logically invalid**, while `Diag(3)`'s is valid - the sharpest form of the paper's thesis, since it shows the same construction failing and succeeding under logically equivalent axiomatizations. `Willard2009` drops it. The codified statement should quote the conference form here, with the drop recorded (drift D47) | `../extraction/willard2006-wollic.md` | discharged:2026-09-01 (C16 R5)|
| O70 | present | **`Willard2009` Theorem 5's deferred source is now extracted.** Discharged 2026-08-29: `Willard2007-APAL` at full depth supplies Definition 1's eight rules, Passive Induction, and Theorems 5–6 (the Ax-1/Ax-2 tableaux negative that Appendix B generalizes). Theorem 5 itself remains `sketch` *in Willard2009* (printed Proof Sketch; details omitted), which is the correct status — not `full`. Gap **G32** closed; drift D48 updated. Pair with O64 | `../extraction/willard2007-apal.md`; `../extraction/willard2009.md` §4 | discharged:2026-09-01 (C16 R5)|
| O71 | present | **State the two-branch split once, and record both namings.** `Willard1993-TR` p. 2 derives the corpus's two lines from Solovay's theorem as Propositions 1 and 7; `Willard2016` Example 3.3b derives the same split from statement `++` as Type-NS-under-Hilbert versus Type-A-under-tableaux. Neither text mentions the other. The codified statement's architecture chapter should carry one derivation with a naming table, and add `Willard2016`'s third datum - that Willard [52,57] with Adamowicz-Zbierski showed **Type-M systems cannot recognize their tableaux consistency**, closing the remaining cell. Drift D49 | `../extraction/willard2016.md` sections 3-4 | discharged:2026-09-01 (C16 R5)|
| O72 | present | **Frame the programme with Willard's own Q-1 / Q-2 split.** `Willard2016` Remark 4.9 divides Hilbert's Second Open Problem into **Q-1** ("Are any axiom systems able to prove theorems verifying their own consistency in a **robust** sense?", answered No by Godel 1931 + Hilbert-Bernays + `++`) and **Q-2** ("Can logic systems **appreciate** their own consistency in some REDUCED sense, that is diluted but not fully immaterial?"). Everything the corpus proves is an answer to Q-2, and saying so at the outset is the most honest available framing - it is Willard's own, and it forecloses the over-reading the whole programme invites. Pairs with O24, O35 and O65 | `../extraction/willard2016.md` section 4 | discharged:2026-09-01 (C16 R5)|
| O73 | present | **`Willard2014` §§5-6 carry the finite Group-2 result (`IS_D^#(β)`).** Discharged 2026-08-29 by hybrid extraction: motivation ruling kept for framing; §§5-6 at full core depth in `extraction/willard2014.md`. Printed name is `IS_D^#(β)`, not Rem 7.3's `ISD(β)` (drift **D76**). Systems/results/notation rows added. Gap **G33** closed | `../extraction/willard2014.md` §§3-4; drift D76 | discharged:2026-09-01 (C16 R5)|
| O74 | present | **Quote `Willard2007-APAL`'s statement of the axiom-versus-theorem distinction; it is the sharpest in the corpus.** p. 29: `Willard2009`'s system "is even able to prove the theorem that multiplication is a total function, in a context where [45]'s main result would **collapse entirely** if such a statement about multiplication's totality property was merely changed from being a **theorem** into becoming an **axiom**." Drift D29 records the distinction; this is the sentence that makes it unmistakable, and it is in a Tier C paper that a lighter treatment would have skipped | `../extraction/willard2007-apal.md` section 2 | discharged:2026-09-01 (C16 R5)|
| O75 | present | **Route the reader to `Willard2001` Appendix A for Solovay's theorem.** `Willard2006-WoLLIC` [26] states that Appendix A of `Willard2001` "offers a **4-page interpretation of the fundamental intuition behind Solovay's idea**". Solovay's result is never published by Solovay, is cited by every paper in the corpus, and forces the two-branch architecture (O71, drift D49). The codified statement must present it, and the corpus contains exactly one extended exposition of it | `../extraction/willard2006-wollic.md` | discharged:2026-09-01 (C16 R5)|
| O76 | present | **Give Willard's own one-sentence explanation of where the tableaux boundary sits and why.** `Willard2000-TAB` section 5: the negative result "breaks down when Multiplication is changed from a total function into a 3-way relation … essentially that **Lemma 8 and its short proof then become no longer valid** … Without it, our proof … **collapses entirely!**" Lemma 8 is the `u_0 = 2`, `u_(i+1) = (u_i)^2`, `z < u_n <= z^2` construction in `O{LogLog(z)}` nodes. So the doubling/squaring device (O44, O50) is not merely how the *positive* systems are built - it is also exactly what the *negative* proofs consume, and removing multiplication-totality starves them. That single observation unifies the affirmative and negative halves of the corpus, and it is stated only here, in a final-remarks paragraph of a 16-page conference paper | `../extraction/willard2000-tab.md` | discharged:2026-09-01 (C16 R5)|
| O77 | present | **Carry Godel's own late view, with its provenance.** `Willard2020-LFCS` section 4 and footnote 1 record Gerald Sacks - Godel's assistant at the IAS for a year - stating in a 2 June 2014 lecture that Godel told him during **1961-1962** that Godel "did not think" Hilbert's Consistency Program's goals "were erased" by the Incompleteness Theorem and believed it left the program "very much alive and even more interesting than it initially was". With Godel's published 1931 caveat (statement \*\*) and Hilbert's 1926 statement (\*), this is the third leg of the motivation, and the only one that is *not* in print from the principals - Willard notes Godel "published fewer than 85 pages in his life" and "never explicitly recorded, during the second half of his life, his partial reluctance". Present it as a recollection, attributed, not as a documented position. Harvey Friedman's 14 March 2014 lecture is a second such datum | `../extraction/willard2020-lfcs.md` | discharged:2026-09-01 (C16 R5)|
| O78 | present | **Disambiguate "cut" once, early, in Willard's own words.** The corpus uses the word in two unrelated senses - Gentzen's sequent-calculus **cut rule** (whose absence defines the cut-free apparatuses, and whose recovery is what Xtab, TabList/Herb-k and Passive Induction all achieve) and a **Definable Cut** (a formula closed downward and under successor, the Nelson-Pudlak-Wilkie-Paris device). `Willard2009` p. 16 is the only place he says they are "**formally unrelated** … despite their very similar sounding names", and adds the fact that makes definable cuts ubiquitous: "all axioms systems, strictly weaker than Peano Arithmetic, contain some definable cut that is not provably equivalent to the full set of integers". A reader who conflates the two will misread D42, O43 and the entire Hilbert-line/tableaux-line split | `../extraction/willard2009.md` section 4 | discharged:2026-09-01 (C16 R5)|
| O79 | present | **Put the "almost cheating" objection in the codified statement, and answer it as Willard does.** `Willard2016` section 8 raises it in his own words - statement ### - and he added that section late, expressly so "the exact meaning of Theorem 6.7's central result could not be confused". His answer is Definition 8.1's **Platonic Stability**: a thinker does not need a sophisticated justification, only a formalism in which presuming its own consistency provably will not spin it into inconsistency. This is the fourth and most direct of Willard's self-limiting statements, alongside O24 (the 1-line-proof caveat), O35 (no ideal system), O65 (the section 9 summary judgement) and O72 (the Q-1/Q-2 split). A codified statement that presents the affirmative results without ### is not faithful to its source | `../extraction/willard2016.md` section 4 | discharged:2026-09-01 (C16 R5)|
| O80 | present | **Record that the LEM result is apparatus-general, and how far that is established.** `Willard2020` section 8 states that Theorems 4.4 and 4.5 **generalize to resolution**: `IS_Res(•)` preserves consistency, `IS_Xres(•)` does not, where `Xres` extends `Res` with all LEM instances as logical axioms exactly as `Xtab` extends `Tab`. No proof is given. The result matters because it separates the phenomenon from semantic tableaux specifically - the corpus's affirmative results are otherwise always stated for one apparatus at a time - but it must be carried as `stated-only`, and resolution belongs in the apparatus axis at C13 alongside `Tab`, `Xtab`, `Tab-1`, Herbrand and the three Hilbert-Frege operators | `../extraction/willard2020.md` | discharged:2026-09-01 (C16 R5)|
| O90 | distinguish | **Truncated floating-point `×` versus `LongMult` is the floating-point boundary dial.** `Willard2006b` Theorem 2 affirmatively lets Def. 4's truncated multiplication be total under Tab-1 self-justification; Theorem 6 forbids Def. 6's untruncated `LongMult`. Remark 3's `R_0=2.0`, `R_{i+1}=R_i*R_i` example is the computational content: Def. 4 stores `2^{2^n}` in `k+n` bits, Def. 6 needs `O(k·2^n)`. This is the corpus's **only** affirmative multiplication-totality result for a self-justifying system, and it must never be stated as "multiplication is total" without the truncated-mantissa qualifier. Cross-ref G3/G36 (the unpublished TR may hold a fuller write-up). **Amended 2026-08-29**: the same dial is already present in `Willard2005-TAB` Theorems 3/6 (positive, truncated) versus Theorem 5 (LongMult negative) and Theorem 4 (AddComp / Hilbert negative) | `../extraction/willard2006b.md` section 7; `../extraction/willard2005-tab.md` §2; drift D70 | carried:2026-09-01 (C16 R5; Refinement)|
| O91 | present | **Carry Willard's Numerical-Analysis versus Number-Theory framing.** Remarks 1–2 claim that Numerical Analysis Cauchy-sequence / efficiency theorems fall in Tier(1)^oplus (real bounds reach essentially `R^J`) while Number Theory theorems almost never fall in Tier(1) (integer bounds are `K·Max(v̄)`). Tab-1 modus ponens then applies to the translated Tier(1) forms via Theorem 3. Present as Willard's own account of *why* the floating-point exception matters — attributed, in the motivation chapter — not as a verified claim about either field. **Amended 2026-08-29**: the same framing is already argued at length in `Willard2005-TAB` pp. 13–14 ("On the Surprising Combined Implications of Theorems 3, 4, 6 and 7") | `../extraction/willard2006b.md` Remarks 1-2; `../extraction/willard2005-tab.md` §2 | carried:2026-09-01 (C16 R5; Refinement)|
| O92 | present | **Do not present `Willard2005-TAB` Theorem 6 as a mere restatement of Theorem 3.** The Important Comment on p. 12 says the opposite: Theorem 6's value is the **Tier(1)^⊕ floating-point** property, which Theorem 7 makes available by translating bounded-real quantifiers into ordinary Tier(1). Without that translation, Tab-1's Gentzen-like cut is as narrow for integer arithmetic as the paper itself argues on p. 13. Pairs with O91 | `../extraction/willard2005-tab.md` §§2,4; drift D70 | carried:2026-09-01 (C16 R5; Refinement)|
| O93 | present | **Carry the deferred "longer version" proofs as open, not as discharged by the journal form.** `Willard2005-TAB` Lemma 1 (Δ*_0 compression for ×/÷), Theorem 4's derivation from Eq. (13), and Theorem 7's LinH argument are all deferred. `Willard2006b` supplies a short sketch for the Tier(1)^⊕ translation (its Theorem 3) but does not claim to discharge the Lemma 1 meticulous analysis or the Theorem 4 details. Gap **G37**; the ASL-2005 TR (**G3**/**G36**) remains the only candidate for a full-length write-up | `../extraction/willard2005-tab.md` §7; gap G37 | cited-only:2026-09-01 (C16 R5; see C16-R5-dispositions.md)|
| O94 | present | **Cite O36 when discussing the 2008 ZCF drafts.** The finite-set reading of SJAS (`ISTR(A)` Bitwise-Or / union) starts in `Willard2001` Remark 3.8, not in the March 2008 ZF-inconsistency notarizations. Present `dew-2008-zcf-group` as a later, unpublished set-theory program that *applies* related intuitions, never as the origin of the finite-set SJAS reading | `../extraction/dew-2008-zcf-group.md`; O36 | discharged:2026-09-01 (C16 R5)|
| O95 | provenance | **Carry the Willard2021 title-page / referee witnesses from C12.** `dew-2025-tab-xtab-notes` p.9 prints the JLC-2021 "Fine Line" title; `dew-2025-boundary-draft` p.1 is the JLC referee report preferring fine/delicate/subtle over "tender". Until G1's published text is acquired, these are the strongest local anchors for the tender→fine title shift | gaps G1; drift D78 | discharged:2026-09-01 (C16 R5)|
| O96 | present | **Present `Willard1998` as DIMACS reflection/NP prehistory, not as the ISTR-proof carrier.** ISREF / Tangibility Reflection / the apparatus↔totality table debut here; ISTR proofs remain on `Willard1997` (G2/G17). The Cognitive Conjecture, `ISREF^R`, Theorems 9.5–9.6 (P=?NP linkage), and cascade-self-verifying / `ISTM^λ` are essentially unique to this paper in the witnessed corpus — carry them as attributed 1998 program statements, not as later-confirmed theorems. Record TangPred / Root printing deltas vs `Willard2001` (drifts **D79**, **D80**) | `../extraction/willard1998.md`; D79; D80; G2 | carried:2026-09-01 (C16 R5; Refinement)|
